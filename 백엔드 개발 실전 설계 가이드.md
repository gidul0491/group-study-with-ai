# 백엔드 개발 실전 설계 가이드

# 웹 애플리케이션 설계 규칙

-   **적용** RDBMS 기반 웹 애플리케이션
    
-   **인용** 2-4-3 처럼 절·항 번호로
    

## 목차

1.  값의 정본
    
2.  데이터베이스 설계
    
3.  민감정보 저장
    
4.  코드 구조와 규약
    
5.  백엔드 계층
    
6.  API 규약
    
7.  테스트
    
8.  외부 연동 명세서 양식
    

이 문서는 웹 애플리케이션의 스키마와 서버 코드를 어떤 규칙으로 만들지 정한 것입니다. 테이블을 어디까지 쪼개고, 이름을 어떻게 짓고, 코드를 어느 계층에 두고, API를 어떤 모양으로 내보내는지를 다룹니다.

규칙마다 왜 그런지를 함께 적었습니다. 이유를 모르면 상황이 달라졌을 때 그 규칙을 언제 깨도 되는지 판단할 수 없기 때문입니다.

조직의 운영 방식에 따라 어느 쪽이 맞는지가 갈리는 항목이 있습니다. 그런 자리에는 한쪽을 정해 두는 대신 양쪽의 판단 근거를 적었습니다. 프로젝트를 시작할 때 한쪽으로 정하고, 정한 것을 전체에서 지키면 됩니다.

규칙 자체는 특정 스택에 기대지 않게 쓰고, 예시와 코드 조각만 Node.js·Express·MySQL 8 기준으로 답니다.

## 01\. 값의 정본

이 문서의 규칙 중 상당수가 하나의 원칙에서 나옵니다. 어떤 사실이든 진짜로 믿을 곳을 한 군데로 정한다는 것입니다. 이 문서에서는 그 한 군데를 정본이라고 부릅니다. 널리 쓰이는 이름은 SSOT(Single Source of Truth)입니다.

같은 사실이 두 곳에 저장되어 있으면 처음에는 같지만 시간이 지나면 반드시 갈립니다. 한쪽만 고치는 경로가 언젠가 생기기 때문입니다.

이것이 지켜지지 않으면 세 곳에서 문제가 나타납니다.

1.  점검할 때 — 두 값이 다를 때 어느 쪽이 맞는지 판단할 근거가 없습니다. 코드를 읽어 "어느 경로가 마지막에 썼는가"를 추적해야 하는데, 경로가 셋 이상이면 사실상 결론이 나지 않습니다.
    
2.  고칠 때 — 잘못된 값을 고쳐도 다음 배치나 다음 저장이 다른 쪽 값으로 되돌립니다. 같은 문의가 반복해서 들어오고, 매번 새로 조사하게 됩니다.
    
3.  운영할 때 — 담당자가 화면에서 본 숫자와 정산 자료의 숫자가 다르면, 그 시점부터 사람은 시스템 대신 자기 엑셀을 믿기 시작합니다. 한 번 그렇게 되면 시스템에 입력하는 일 자체가 형식적인 절차가 됩니다.
    

### 1-1 정본을 정하는 기준

1.  **원본에서 세거나 계산해 낼 수 있는 값은 저장하지 않습니다.**
    
    `_count`나 `_yn`처럼 개수와 여부를 따로 저장하는 컬럼이 대표적입니다. 원본이 바뀔 때 이 컬럼을 함께 고치는 경로가 하나라도 빠지면, 목록 화면과 집계 화면이 다른 숫자를 보여줍니다. 이때 정본은 원본이고, 저장해 둔 값은 복사본입니다.
    
2.  **나중에 다시 계산했을 때 값이 달라지면 안 되는 값은, 계산하지 않고 그때의 값을 저장합니다.**
    
    조인해서 가져오면 원본이 바뀔 때 과거 기록까지 따라 바뀝니다. 처리자 이름을 조인으로 가져오면, 그 계정이 지워졌을 때 과거 기록의 이름이 빈칸이 되고 개명하면 과거 이름까지 바뀝니다. 이런 값은 참조 컬럼과 함께 그때 화면에 보이던 값도 컬럼에 저장하고, 스냅샷이라고 주석에 적습니다. 이것은 원칙을 어기는 것이 아닙니다. **정본이 원본 테이블에서 저장된 그 컬럼 자신으로 옮겨 간 것**이고, 계산식은 값을 처음 만들 때 한 번만 쓰입니다. 이 구분이 없으면 "중복이니까 빼라"로 스냅샷까지 지우게 됩니다.
    
3.  **둘 중 어느 쪽인지는 "나중에 다시 계산했을 때 값이 달라져도 되는가"로 판단합니다.**
    
    달라져도 되면 저장하지 않고, 달라지면 안 되면 저장합니다. 판단이 서지 않으면 저장하지 않는 쪽을 고릅니다. 나중에 저장으로 바꾸는 것은 가능하지만, 이미 저장해 둔 틀린 값을 되살리는 것은 불가능합니다.
    
4.  **로직에도 같은 원칙을 적용합니다. 값을 만들어 내는 계산이 두 곳에 있으면 그 값의 정본도 두 곳이 됩니다.**
    
    테이블은 잘 쪼개 놓고 같은 계산을 화면마다 다시 짜면, 스키마를 정리한 효과가 사라집니다. 같은 데이터인데 화면마다 다른 값이 나오는 원인이 대부분 이것입니다.
    

## 02\. 데이터베이스 설계

### 2-1 정규화

정규화는 한 테이블에 여러 종류의 사실을 담지 않도록 테이블을 쪼개는 작업입니다. 1장의 정본 원칙을 한 테이블 안에서 적용하는 도구입니다.

보는 범위가 한 테이블 안으로 한정된다는 점은 알아 두어야 합니다. 고객 테이블의 주문건수 컬럼은 정규형 검사를 전부 통과하지만 정본은 갈립니다. 다른 테이블에서 계산해 낼 수 있는 값인지는 정규화가 보지 않고, 1-1-1이 봅니다.

1.  **제1정규형 — 한 칸에는 값 하나만 담습니다. 같은 성격의 컬럼을 번호를 붙여 늘어놓지 않습니다.**
    
    한 칸에 여러 값을 넣으면 그 안의 값 하나로 조회할 수 없고, 인덱스도 걸리지 않습니다. 번호를 붙인 컬럼은 개수가 늘어날 때마다 스키마를 고쳐야 하고, 몇 번째 칸이 비었는지에 따라 조회 조건이 달라집니다. 여러 개가 달릴 수 있는 값은 별도 테이블로 뺍니다. 다만 한 칸에 담는 것이 정당해지는 경우가 있고, 그 조건은 2-2-4에 있습니다.
    
    ```
    이런 형태를 만들지 않습니다
      driver.phone1, driver.phone2, driver.phone3
      driver.phones = '010-1111-2222,010-3333-4444'
    
    이렇게 나눕니다
      driver              id · name
      driver_phone        id · driver_id · phone · is_primary
    ```
    
2.  **제2·제3·보이스-코드정규형 — "이 컬럼은 무엇이 정해지면 따라 정해지는가"를 묻고, 그 답이 이 테이블의 키가 아니면 그 답이 키인 테이블로 보냅니다.**
    
    컬럼마다 이 질문 하나만 던지면 됩니다. 주문상품 테이블의 상품명은 상품번호 하나로 정해지고, 주문 테이블의 고객등급은 고객번호로 정해집니다. 둘 다 그 테이블의 키가 아닙니다. 그대로 두면 상품명은 같은 상품이 팔릴 때마다 복사되고 상품명을 고칠 때 이미 쌓인 행은 옛 이름으로 남습니다. 고객등급은 등급을 올릴 때 그 고객의 과거 주문 행을 전부 고쳐야 하고, 한 건이라도 빠지면 같은 고객의 등급이 주문마다 다르게 남습니다. 다만 주문 시점의 단가처럼 그때 값이 굳어야 하는 컬럼은 이 검사에 걸려도 남깁니다 — 1-1-2.
    
    ```
    order_product   order_id · product_id · quantity · unit_price
                    product_name     ← 상품번호로 정해진다  → product 로
    
    order           id · customer_id · ordered_at · total_amount
                    customer_grade   ← 고객번호로 정해진다  → customer 로
    ```
    

> **참고** — 복합키의 일부로 정해지면 제2정규형, 키가 아닌 다른 컬럼으로 정해지면 제3정규형, 그리고 제3정규형이 남겨 두는 예외까지 막은 것이 보이스-코드 정규형입니다. 셋 다 "정하는 쪽이 이 테이블의 키인가"를 묻는 것이라 2번 규칙 하나로 통합됩니다. 설계할 때 이름을 외울 필요는 없지만, 다른 자료를 읽거나 DBA와 이야기할 때는 이 말로 통합니다. 그 위의 제4·제5정규형은 성격이 다릅니다. 서로 무관한 다중값을 한 테이블에 담는 경우를 다루는데, 두 항을 모두 지킨 스키마에서 걸리는 일은 드뭅니다.

### 2-2 반정규화와 JSON 컬럼을 허용하는 조건

1.  **성능 문제가 실제로 측정된 뒤에만 반정규화합니다.**
    
    느릴 것 같다는 예상으로 미리 복사해 두면, 느리지도 않은 자리에 갈릴 수 있는 값만 늘립니다. 대부분의 조인 성능 문제는 인덱스(2-8)로 해결되고, 반정규화는 그것으로 안 될 때의 수단입니다.
    
2.  **반정규화한 컬럼에는 원본이 어디인지와 언제 다시 만드는지를 주석에 적습니다.**
    
    적어 두지 않으면 몇 달 뒤에 그 컬럼이 정본인지 복사본인지 알 수 없습니다. 값이 틀렸을 때 어디를 고쳐야 하는지도 답할 수 없습니다.
    
3.  **읽기 전용 집계는 컬럼을 늘리는 대신 별도 집계 테이블이나 뷰로 두는 쪽을 먼저 봅니다.**
    
    업무 테이블에 집계 컬럼을 섞으면 그 테이블에 쓰기가 일어날 때마다 집계를 함께 맞춰야 합니다. 따로 두면 맞추는 시점을 정할 수 있고, 틀렸을 때도 업무 데이터는 멀쩡합니다.
    
4.  **한 칸에 여러 값을 담기 전에, 그 원소가 독립적으로 다뤄지는 대상인지 봅니다.**
    
    제1정규형을 깨서 실제로 잃는 것은 세 가지입니다. 원소 하나로 조회하거나 조인할 수 없고, 원소 하나만 수정할 수 없어 통째로 덮어써야 하고, 원소에 참조 무결성을 걸 수 없습니다. 이 셋을 쓰지 않는 값이면 JSON 컬럼에 담아도 됩니다. 하나라도 쓰면 테이블로 나눕니다. 판단 기준은 정규형 번호가 아니라 이 세 가지입니다.
    
5.  **JSON 컬럼은 구조를 미리 적을 수 없는 값에 씁니다.**
    
    행마다 구조가 다르고, 통째로 읽고 통째로 쓰고, 안의 값으로 검색하거나 집계하지 않는 값입니다. 외부 API의 원본 응답, 이벤트 페이로드, 변경 이력의 이전·이후 스냅샷이 여기 해당합니다. 구조를 적을 수 있는 값은 컬럼으로 적습니다. 스키마를 정하기 귀찮아서 JSON에 담는 것과, 정할 수 없어서 담는 것은 다릅니다.
    

### 2-3 테이블명·컬럼명

1.  **테이블명과 컬럼명은 스네이크 케이스로 씁니다.**
    
    대소문자를 구분하는지는 DB 제품과 운영체제마다 다릅니다. 나중에 DB를 옮기거나 다른 도구가 이 테이블을 읽을 때, 대소문자에 기대지 않는 이름이라야 손대지 않고 그대로 쓸 수 있습니다.
    
2.  **테이블명은 단수형으로 씁니다.**
    
    테이블 이름은 그 안의 한 행을 가리킵니다. 기사 테이블을 `driver`로 지으면 참조 컬럼은 `driver_id`, 연결 테이블은 `company_driver`로 자연스럽게 이어집니다. `drivers`로 지으면 `drivers_id`가 어색해 결국 단수로 쓰게 되고, 테이블명과 컬럼명이 어긋납니다.
    
3.  **분류를 나타내는 말을 이름에 넣지 않습니다 —** `master`**,** `tbl`**,** `info`**,** `data` **등.**
    
    `master`를 붙여 두면 그 테이블이 더 이상 기준 테이블이 아니게 돼도 이름은 남습니다. `tbl`이나 `info`는 어느 테이블에나 붙일 수 있어서 구분에 쓸모가 없습니다.
    
4.  **단어를 축약하지 않습니다.**
    
    줄이는 방식이 사람마다 다릅니다. `insurance`를 누구는 `insur`로, 누구는 `ins`로 줄여 같은 개념에 두세 개의 이름이 생깁니다. 나중에 통일하려면 DB와 서버와 화면을 함께 고쳐야 합니다.
    
5.  **같은 개념에는 같은 이름을, 다른 개념에는 다른 이름을 씁니다.**
    
    같은 값인데 테이블마다 `start_date`와 `begin_at`처럼 이름이 다르면 조인할 때마다 어느 쪽이 어느 쪽인지 확인해야 합니다. 반대로 다른 값인데 이름이 같으면 조인할 때 한쪽 값이 다른 쪽을 덮습니다. 계약 테이블과 그 계약에 등록된 사람 테이블이 있을 때 계약의 기간과 사람이 등록되어 있는 기간은 서로 다릅니다. 둘 다 `start_at`·`end_at`으로 지으면 조인해 한 화면에 내보낼 때 어느 쪽 기간인지 알 수 없습니다. 계약 쪽은 `contract_start_at`, 사람 쪽은 `enrolled_start_at`처럼 이름을 갈라야 합니다.
    
6.  **다른 테이블을 참조하는 컬럼은** `참조테이블명_id`**로 짓습니다.**
    
    기사를 참조하면 `driver_id`, 업체를 참조하면 `company_id`입니다. 이름만 보고 어느 테이블을 가리키는지 알 수 있습니다.
    
7.  **한 테이블에서 같은 대상을 두 번 이상 참조하면 역할을 앞에 붙입니다.**
    
    담당자 테이블을 접수자와 처리자로 두 번 참조한다면 `receiver_admin_user_id`와 `handler_admin_user_id`가 됩니다. `admin_user_id_2` 같은 이름은 어느 쪽이 무엇인지 알려주지 않습니다.
    
8.  **기본키 컬럼명은** `id`**로 통일합니다.**
    
    테이블마다 다르면 공통 조회·매핑 코드를 쓸 수 없고, 테이블 이름을 인자로 받는 함수마다 예외를 둬야 합니다.
    
9.  **시각을 담는 컬럼은** `_at`**으로 끝냅니다 —** `created_at`**,** `contract_start_at`**.**
    
    이름만 보고 그 컬럼에 시각이 들어 있다는 것을 알 수 있습니다.
    
10.  **연결 테이블 이름은 두 테이블 이름을 이어 붙이고, 순서를 프로젝트 전체에서 하나로 고정합니다.**
     

`company_driver`와 `driver_company`가 함께 생기면 찾을 때마다 양쪽을 다 뒤져야 합니다. 부모 쪽을 앞에 두거나 알파벳 순으로 두거나, 어느 쪽이든 한 가지로 정합니다.

### 2-4 타입

1.  **같은 개념을 담는 컬럼은 스키마 전체에서 하나의 타입만 씁니다.**
    
    한쪽에서 읽은 값을 다른 쪽에 넣을 때 잘리거나 오류가 납니다. 조인이나 비교에서도 형변환이 일어나 인덱스를 타지 못합니다. 참조 컬럼은 참조 대상 기본키와 타입도 부호도 똑같이 맞춥니다.
    
2.  **소수점이 있는 값은** `decimal`**로 담습니다. 부동소수점(**`float`**,** `double`**)은 쓰지 않습니다.**
    
    부동소수점은 0.1 같은 십진 소수를 이진수로 정확히 표현하지 못해, 미세하게 어긋난 값이 저장됩니다. 금액이나 요율처럼 합계와 비교가 중요한 값에서 그 차이가 드러납니다.
    
3.  **기간의 시작과 종료 컬럼은** `datetime`**을 기본으로 하고, 업무가 날짜 단위로만 돌아가는 기간은** `date`**로 둡니다.**
    
    계약 효력이나 접수 기간처럼 같은 날 안에서도 시각으로 앞뒤가 갈리는 기간은 `datetime`이어야 합니다. `date`로 두면 당일에 시작하고 당일에 끝난 두 건의 순서를 가릴 수 없습니다. 반대로 숙박일이나 회계기간처럼 시각 개념이 아예 없는 기간까지 `datetime`으로 두면, 실제로는 쓰지 않는 `00:00:00`을 매번 붙이게 되고 종료일에 `23:59:59`를 넣는 코드가 여기저기 생깁니다. 어느 쪽을 쓰든 종료일이 포함인지 아닌지를 컬럼 주석에 적습니다. 이것을 적지 않아서 생기는 하루치 차이가 기간 계산 오류의 대부분입니다.
    
4.  **기본키 타입은** `int`**를 기본으로 하고, 행이 21억을 넘길 수 있는 테이블만** `int unsigned`**나** `bigint`**를 씁니다.**
    
    `int`는 약 21억, `int unsigned`는 약 42억까지 담습니다. 로그나 이력처럼 하루에 수십만 행이 쌓이는 테이블이 아니면 넘길 일이 없습니다. `bigint`를 기본으로 삼지 않는 이유는 크기입니다. 기본키는 4바이트에서 8바이트가 되고, 그 값을 참조하는 모든 컬럼과 인덱스가 함께 두 배가 됩니다. 다만 값이 9,007,199,254,740,991(2의 53승)을 넘어가면 자바스크립트가 정수를 그대로 다루지 못하므로, `bigint`를 쓰면서 그 범위까지 실제로 채우는 테이블은 API 응답에서 문자열로 내보냅니다.
    
5.  **문자셋은** `utf8mb4`**로 하고, 정렬 규칙은 무엇을 쓰든 DB 전체에서 하나로 통일합니다.**
    
    중요한 것은 어느 규칙을 고르느냐가 아니라 섞이지 않는 것입니다. 정렬 규칙이 다른 두 컬럼을 조인하면 인덱스를 타지 못하고, 조건에 따라 오류가 납니다. 3바이트 문자셋이 섞이면 이모지가 저장 시점에 잘립니다. 새로 만드는 DB라면 MySQL 8의 기본값인 `utf8mb4_0900_ai_ci`를 그대로 쓰고, 이미 도는 DB에 테이블을 더하는 것이라면 그 DB가 쓰는 규칙에 맞춥니다. 기본값이 낫다는 이유로 기존 DB에 다른 규칙을 섞는 것이 가장 나쁩니다.
    
6.  **문자열 컬럼은 길이를 넉넉하게 잡되, 인덱스가 걸리거나 정렬에 쓰이는 컬럼은 실제 최대치에 여유만 더해 잡습니다.**
    
    `varchar`는 선언한 길이가 아니라 실제 들어간 값의 길이만큼만 저장합니다. 그래서 저장 공간만 보면 짧게 잡아 아끼는 것이 없고, 나중에 늘리려면 행이 많은 테이블은 다시 써야 하니 넉넉한 쪽이 낫습니다. 다만 인덱스와 정렬은 다릅니다. 인덱스 키에는 선언 길이가 그대로 잡혀 한 인덱스에 넣을 수 있는 컬럼 수와 길이에 한계가 생기고, 정렬이나 임시 테이블을 만들 때도 선언 길이만큼 메모리를 먼저 잡습니다. `varchar(1000)` 컬럼으로 정렬하는 조회는 실제 값이 열 글자여도 그만큼을 씁니다. 조회 조건과 정렬에 쓰이지 않는 컬럼은 넉넉하게, 쓰이는 컬럼은 필요한 만큼으로 나눠 잡습니다.
    
7.  **DB와 서버가 쓰는 시간대를 하나로 고정하고 그 값을 설정에 명시합니다.**
    
    시간대를 정하지 않으면 서버가 어디서 도는지에 따라 저장되는 값이 달라집니다. 배포 환경이 바뀐 날부터 몇 시간씩 어긋난 행이 섞이고, 섞인 뒤에는 어느 행이 어느 시간대로 저장됐는지 구분할 방법이 없습니다.
    
8.  **금액 컬럼은 단위와 절사 규칙을 주석에 적습니다.**
    
    원 단위인지 전 단위인지가 정해져 있지 않으면 화면과 정산이 100배 다른 값을 다루게 됩니다. 어디서 절사하고 어디서 반올림하는지도 함께 적습니다.
    

### 2-5 키와 관계 표현

1.  **기본키는 업무 의미가 없는 값으로 둡니다. 업무상 식별자는 별도 컬럼에 담습니다.**
    
    사업자번호나 계약번호처럼 업무에서 쓰는 식별자는 규칙이 바뀌거나 잘못 입력됩니다. 그것이 기본키면 값 하나를 고치기 위해 참조하는 모든 테이블을 함께 고쳐야 합니다. 업무 식별자는 일반 컬럼에 두고 필요하면 유니크 제약으로 막습니다.
    
2.  **일대다 관계는 자식 테이블에 부모의** `id`**를 둡니다.**
    
    부모에 자식 목록을 담으려 하면 2-1-1에 걸립니다. 자식이 몇 개가 되든 스키마는 그대로여야 합니다.
    
3.  **다대다 관계는 연결 테이블로 표현합니다.**
    
    양쪽 테이블 어디에도 상대의 목록을 담지 않습니다. 연결 테이블은 두 참조 컬럼만 갖는 것이 기본이고, 관계 자체에 딸린 값이 있으면 여기에 함께 둡니다. 시작일·종료일·상태가 그런 값입니다.
    
4.  **관계에 딸린 값을 양쪽 테이블 어디에도 두지 않습니다.**
    
    업체와 증권이 다대다일 때 납부일이나 사용 여부는 업체의 값도 증권의 값도 아니고 그 둘의 연결에 딸린 값입니다. 한쪽에 두면 같은 업체가 증권마다 다른 납부일을 가질 수 없거나, 값이 여러 행에 복사됩니다.
    
5.  **한 컬럼이 상황에 따라 다른 테이블을 가리키게 만들지 않습니다.**
    
    대상 테이블을 담는 컬럼과 대상 id를 담는 컬럼을 두어 여러 테이블을 가리키는 방식입니다. 조인을 조건 없이 걸 수 없고, 참조 무결성을 어떤 방법으로도 걸 수 없으며, 대상이 하나 늘 때마다 그것을 다루는 코드를 전부 찾아 고쳐야 합니다. 대상별로 컬럼이나 테이블을 나누는 편이 낫습니다.
    
6.  **계층 구조는 부모** `id` **하나만 두고 끝내지 않습니다. 하위 전체를 조회할 일이 있으면 조회용 구조를 함께 둡니다.**
    
    조직도, 상품 카테고리, 지역 코드, 댓글 스레드가 여기 해당합니다. 부모 `id`만 있으면 한 단계 위아래는 쉽지만 "이 부서 아래 전부"는 깊이만큼 반복 조회가 됩니다. 재귀 CTE로 한 문장에 담을 수는 있어도 깊이만큼 도는 것은 같습니다. 트리를 그리는 화면이 열릴 때마다 이 조회가 나갑니다.
    
    ```
    부모 id 만 두는 방식
      department   id · parent_id · name
    
    경로를 함께 저장
      department   id · parent_id · name · path      '/1/4/9/'
      하위 전부 = WHERE path LIKE '/1/4/%'
                  ← 앞에 와일드카드가 없어 인덱스를 탄다
    
    조상·후손 쌍을 따로 저장
      department          id · parent_id · name
      department_closure  ancestor_id · descendant_id · depth
      하위 전부 = WHERE ancestor_id = 4   ← 조인 한 번
    ```
    
7.  **어느 쪽을 더할지는 트리가 얼마나 자주 바뀌는지로 정합니다.**
    
    경로를 저장하는 방식과 조상·후손 쌍을 저장하는 방식은 조회가 빠른 대신, 노드를 옮길 때 그 아래 행을 전부 갱신해야 합니다. 조직 개편이 몇 년에 한 번이면 그 비용은 없는 것과 같습니다. 반대로 트리가 수시로 바뀌고 조회는 한두 단계뿐이라면 부모 `id`만 두는 편이 낫습니다. 판단이 서지 않으면 부모 `id`로 시작합니다. 부모 `id`에서 경로는 언제든 다시 만들 수 있지만, 그 반대는 안 됩니다. 더하기로 했다면 경로 저장이 기본입니다. 컬럼 하나면 끝나고 행도 늘지 않으며 `ORDER BY`만으로 트리 순서가 나옵니다. 조상 방향 조회나 깊이 조건이 자주 나오거나 한 노드가 부모를 여럿 가질 수 있으면 그때 조상·후손 쌍으로 갑니다.
    
8.  **부모를 지정할 때 순환을 막습니다.**
    
    A의 부모를 B로, B의 부모를 A로 두면 하위를 훑는 조회가 끝나지 않습니다. 자기 자신을 부모로 두는 것도 같습니다. DB 제약으로는 막을 수 없으므로, 부모를 바꾸는 코드에서 새 부모가 자기 자손인지 확인합니다.
    

### 2-6 상태·시각·삭제 표현

1.  **모든 테이블에** `created_at`**,** `updated_at`**,** `deleted_at` **세 컬럼을 둡니다. 쌓기만 하고 고치거나 지우지 않는 이력 테이블은** `created_at`**만 둡니다.**
    
    기본값까지 아래 정의를 그대로 씁니다. 이력 테이블에 나머지 둘을 두면 고치거나 지워도 되는 것처럼 읽힙니다.
    
    ```
    created_at datetime DEFAULT CURRENT_TIMESTAMP
    updated_at datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
    deleted_at datetime DEFAULT NULL   값이 있으면 삭제된 행
    ```
    
2.  **삭제는 행을 지우지 않고** `deleted_at`**에 시각을 넣는 방식으로 합니다.** `use_yn`**,** `is_deleted`**,** `del_flag` **같은 컬럼을 따로 두지 않습니다.**
    
    삭제됐다는 사실이 두 컬럼에 나뉘어 저장되면 한쪽만 갱신하는 코드가 생깁니다. 언제 지워졌는지도 함께 필요한 경우가 대부분이라 시각 컬럼 하나가 두 가지를 답합니다.
    
3.  **여러 값을 오가는 상태는 문자열 상태 코드 컬럼으로 담습니다. 값은 대문자 스네이크로 쓰고(**`IN_PROGRESS`**), 가능한 값 전체를 컬럼 주석에 적습니다.**
    
    숫자 코드는 화면·배치·통계가 각자 해석합니다. 값이 하나 늘어난 것도 코드를 읽어야 알 수 있습니다. 컬럼명과 값의 표기는 서로 다릅니다 — 컬럼명은 `settlement_status`, 그 안에 담기는 값은 `SETTLEMENT_DONE`입니다.
    
4.  `tinyint(1)` **참·거짓 컬럼은 언제 그렇게 됐는지가 의미 없는 설정에만 씁니다.**
    
    기능 사용 여부처럼 언제 켰는지 아무도 묻지 않는 값만 해당합니다. 업무 상태는 언제 그렇게 됐는지를 반드시 묻습니다.
    
5.  **상태가 언제 어떻게 바뀌었는지를 물을 업무라면, 상태 컬럼과 별개로 상태 변경 이력 테이블을 둡니다.**
    
    상태 컬럼은 지금 값만 알려 줍니다. 언제 누가 바꿨는지는 덮어쓰이면서 사라집니다. 나중에 붙이려 하면 이미 지나간 변경은 되살릴 수 없습니다.
    
6.  **이력 테이블은 그 시점의 값을 그대로 담고, 나중에 조인해 채우지 않습니다.**
    
    이력의 존재 이유가 "그때 어땠는가"를 답하는 것입니다. 조인으로 지금 값을 가져오면 그 답을 할 수 없습니다.
    

### 2-7 제약

1.  **참조 무결성과 중복 방지를 DB 제약으로 걸지 애플리케이션 로직으로 막을지 프로젝트를 시작할 때 정합니다. 둘은 따로 정하고, 같은 결론이 나오지 않아도 됩니다.**
    
    양쪽 모두 실제로 쓰이는 방식이고, 어느 쪽이 맞는지는 조직의 운영 방식에 따라 갈립니다. 정하지 않고 시작하면 테이블마다 다른 방식이 되어, 어떤 값이 무엇으로 보호되고 있는지 아무도 답할 수 없게 됩니다. 둘을 갈라 정하는 이유는 깨졌을 때의 회복 가능성이 다르기 때문입니다. 참조가 끊긴 행은 나중에 찾아 이어 붙일 수 있지만, 중복으로 들어간 두 행은 어느 쪽이 진짜인지 판단할 근거가 이미 사라진 뒤입니다. 참조는 로직으로 막고 중복만 DB 제약으로 거는 조합이 실제로 흔하고, 그것은 규칙을 어긴 것이 아닙니다. 다음 두 항이 판단 근거입니다.
    
2.  **DB 제약은 코드를 거치지 않는 경로까지 막습니다.**
    
    배치, 수동 SQL, 같은 DB를 보는 다른 서비스가 그렇습니다. 애플리케이션 로직만으로 막으면 이 경로가 열려 있어, 참조가 깨진 행이 조용히 쌓이다가 조인 결과에서 행이 사라지는 방식으로 나중에 드러납니다.
    
3.  **애플리케이션 로직은 업무에 따라 바뀌는 기준과 운영 중의 예외를 다룰 수 있습니다.**
    
    중복 판정 기준이 업무 조건에 따라 달라지거나, 대량 적재·정합성 보정처럼 일시적으로 규칙 밖의 데이터를 넣어야 하는 운영이 있으면 DB 제약이 걸림돌이 됩니다. 작업할 때마다 제약을 풀었다 거는 운영은 그 자체로 위험합니다.
    
4.  **어느 쪽으로 정하든, 무엇을 무엇으로 막고 있는지 문서에 적습니다.**
    
    DB 제약으로 막았다면 스키마 정의가 그 문서 역할을 하지만, 애플리케이션 로직으로 막았다면 적어 두지 않으면 아무 데도 남지 않습니다. 나중에 그 컬럼을 다루는 사람이 보호 장치가 있다고 착각하거나, 반대로 이미 있는 검사를 한 번 더 만듭니다.
    
5.  **중복이 한 번 생기면 되돌리기 어려운 컬럼은 로직으로 막기로 했더라도 DB 유니크 제약을 함께 고려합니다.**
    
    조회한 뒤 저장하는 코드는 그 사이에 끼어드는 요청을 막지 못합니다. 트랜잭션과 잠금으로도 막을 수 있지만 격리 수준과 인덱스 구성에 따라 동작이 달라지고 교착이 생기기 쉽습니다. 다만 아래 6번의 충돌을 함께 감수할 수 있을 때만 겁니다.
    
6.  **소프트 삭제와 유니크 제약이 겹치면, 삭제된 행이 그 값을 계속 차지하지 않도록 처리 방법을 미리 정합니다.**
    
    삭제한 아이디를 다시 쓸 수 있어야 하는지가 업무 판단입니다. 다시 쓸 수 있어야 한다면 방법은 두 가지입니다. 하나는 삭제 시점에 값 뒤에 표식을 붙이는 것으로, 제약을 그대로 두고 해결되지만 원래 값이 그 컬럼에서 사라집니다. 삭제 전 값을 나중에 확인해야 한다면 별도 컬럼에 함께 남깁니다. 다른 하나는 살아 있는 행에서만 값을 갖는 생성 컬럼을 만들고 거기에 유니크를 거는 것입니다. 삭제된 행은 그 컬럼이 비므로, 유니크 인덱스가 빈 값을 여러 개 허용한다는 성질을 이용해 삭제한 값끼리는 충돌하지 않게 됩니다. 조건을 붙인 인덱스를 지원하는 DB에서는 그 기능으로 한 줄에 끝나지만 MySQL에는 없어 이 두 방법을 씁니다. 정하지 않으면 삭제한 계정 때문에 같은 아이디로 다시 가입되지 않는 문의가 들어옵니다.
    
    ```
    값에 표식을 붙이는 방법
      UPDATE member
         SET login_id = CONCAT(login_id, '#', id), deleted_at = NOW()
       WHERE id = ?
    
    생성 컬럼에 유니크를 거는 방법
      login_id         varchar(50) NOT NULL,
      deleted_at       datetime NULL,
      active_login_id  varchar(50)
        GENERATED ALWAYS AS (IF(deleted_at IS NULL, login_id, NULL)) STORED,
      UNIQUE KEY uk_member_active_login_id (active_login_id)
    ```
    
7.  **DB 제약으로 걸기로 했다면 삭제 동작은** `RESTRICT`**를 기본으로 하고,** `CASCADE`**는 부모가 사라지면 자식도 의미가 없어지는 관계에만 씁니다.**
    
    `CASCADE`는 지운 것보다 훨씬 많은 행을 지웁니다. 소프트 삭제(2-6-2)를 쓰면 애초에 물리 삭제가 일어나지 않으므로 대부분의 관계에서 `RESTRICT`로 충분합니다.
    
8.  `NOT NULL`**은 비어도 되는 이유가 없는 컬럼에 겁니다.**
    
    비어도 되는 이유는 두 가지뿐입니다. 아직 그 단계에 오지 않아 비는 값과, 그 업무에서는 해당이 없어 비는 값입니다. 나머지는 값이 있어야 하고, 빠뜨린 코드는 저장하는 순간 드러나야 합니다. 참조·중복과 달리 이 제약은 업무 기준이 바뀌어도 흔들리지 않아 따로 판단할 것이 없습니다.
    
9.  **값의 범위가 정해진 컬럼에는 기본값을 두지 않습니다. 상태 컬럼이 대표적입니다.**
    
    기본값이 있으면 상태를 넣지 않은 코드가 오류 없이 통과하고, 그 행은 아무도 만들려 하지 않은 상태로 저장됩니다.
    

### 2-8 인덱스

1.  **인덱스는 행이 많고 실제 조회 조건으로 쓰이는 컬럼에만 만듭니다.**
    
    두 조건 중 하나만 만족하는 인덱스는 조회를 빠르게 하지 못하면서 저장할 때마다 비용만 듭니다.
    
2.  **배치나 상주 프로세스가 주기적으로 조회하는 컬럼에도 인덱스를 만듭니다.**
    
    화면 조회와 달리 느려도 아무도 불평하지 않아서 가장 자주 빠뜨립니다. 주기가 짧으면 그 사이 내내 전체 조회가 돕니다.
    
3.  **소프트 삭제를 쓰면 복합 인덱스의 조건 컬럼 뒤에 삭제 시각 컬럼을 넣습니다.**
    
    모든 조회가 삭제 여부 조건을 함께 달고 나갑니다. 인덱스에 없으면 좁혀 낸 행을 하나씩 읽어 삭제 여부를 확인해야 하고, 뒤에 정렬과 개수 제한이 붙으면 20건을 얻으려고 훨씬 많은 행을 읽게 됩니다.
    
4.  **복합 인덱스의 컬럼 순서는 그 인덱스를 쓸 조회 쿼리의 조건절이 정합니다. 조건에 항상 들어가는 컬럼을 앞에, 범위로 좁히는 컬럼을 뒤에 둡니다.**
    
    인덱스는 앞 컬럼부터 차례로 써야 걸립니다. 앞을 건너뛰고 뒤 컬럼만 조건에 넣으면 이 인덱스로는 좁히지 못합니다. 업체별로 기간을 잘라 조회한다면 `(company_id, contract_date)` 순이고, 뒤집으면 업체 조건이 인덱스로 좁혀지지 않습니다. 값이 잘게 나뉘는 컬럼을 앞에 두는 것이 아닙니다.
    
5.  **앞 컬럼이 등호로 고정되고 목록을 늘 같은 순서로 보여준다면, 그 정렬 컬럼을 조건 컬럼 뒤에 붙입니다.**
    
    앞 컬럼이 하나의 값으로 좁혀진 구간 안에서는 인덱스가 이미 정렬 컬럼 순서로 놓여 있어, 읽어 온 뒤 다시 줄 세우는 일을 건너뜁니다. 앞 컬럼이 범위로 열려 있으면 그렇지 않습니다.
    
6.  **조건절의 컬럼에 함수를 씌우지 않습니다.**
    
    `DATE(created_at) = '2026-09-06'`처럼 쓰면 인덱스를 지나칩니다. 범위 조건으로 바꿔 컬럼을 그대로 둡니다.
    
7.  **인덱스를 만들거나 고친 뒤에는 실제 쿼리의 실행 계획을 뽑아 그 인덱스를 쓰는지, 몇 행을 읽는지 봅니다.**
    
    인덱스는 만들어 둔다고 쓰이지 않습니다. 조건에 함수를 씌우거나 타입이 다르면 그냥 지나칩니다. 읽는 행 수는 인덱스가 실제로 좁혔는지를 그대로 보여줍니다.
    
8.  **인덱스 이름에 테이블명과 대상 컬럼을 규칙적으로 넣습니다.**
    
    스키마 정의만 보고도 빠진 인덱스와 중복 인덱스를 찾을 수 있습니다.
    
9.  **개발 단계의 시험 데이터는 실제 예상 규모에 가깝게 만듭니다.**
    
    행이 백 개인 테이블은 인덱스가 없어도 빠릅니다. 인덱스가 빠졌다는 사실은 운영에 데이터가 쌓인 뒤에 드러나고, 그때는 인덱스를 거는 것 자체가 서비스에 부담이 됩니다.
    

### 2-9 주석

1.  **모든 컬럼에 주석을 답니다.**
    
    스키마를 고칠 때 주석도 같은 자리에서 고치게 됩니다. 별도 문서에 적어 두면 스키마만 바뀌고 문서는 그대로 남습니다.
    
2.  **주석에는 업무상 의미를 적습니다. 컬럼명을 한글로 옮겨 적지 않습니다.**
    
    이름을 되풀이하는 주석은 아무것도 더하지 않으면서 주석이 있다는 착각만 만듭니다.
    
3.  **비어 있을 수 있는 컬럼에는 언제 비는지를 적습니다.**
    
    NULL이 "아직 오지 않은 값"인지 "해당 없음"인지에 따라 조회 조건이 달라집니다.
    
4.  **상태 컬럼에는 가능한 값 전체와 각 값의 뜻을 적습니다.**
    
    상태가 몇 가지인지는 코드를 전부 읽지 않으면 알 수 없습니다. 스키마 정의 옆에 있으면 새 상태를 추가할 때 기존 목록을 보고 판단하게 됩니다.
    
5.  **스냅샷 컬럼과 반정규화 컬럼에는 그렇게 저장한 것이라고 적습니다.**
    
    이 표시가 없으면 조인으로 대체할 수 있는 값처럼 보입니다. 정리한다고 지웠다가 과거 기록이 함께 사라집니다.
    

### 2-10 스키마 변경

1.  **스키마 변경은 코드와 같은 저장소에 파일로 남기고, 순서대로 한 번씩만 적용되게 합니다.**
    
    사람이 콘솔에서 직접 친 `ALTER`는 어디에도 남지 않습니다. 개발 DB에만 적용되고 운영에는 빠진 변경이 생기고, 그 사실은 운영에서 오류가 날 때 드러납니다. 파일로 두면 어느 환경에 어디까지 적용됐는지가 값으로 남습니다.
    
2.  **이미 운영 중인 테이블에 컬럼을 더할 때는 비어도 되는 상태로 시작합니다.** `NOT NULL`**은 값을 채운 뒤에 겁니다.**
    
    한 번에 `NOT NULL`로 만들면 기존 행 때문에 변경 자체가 실패하거나, 아무도 원하지 않는 기본값이 전부 채워집니다. 컬럼 추가 · 값 채우기 · 제약 걸기를 세 번으로 나눕니다.
    
3.  **컬럼이나 테이블의 이름은 바꾸지 않습니다. 새로 만들어 양쪽에 쓰다가 옛것을 지웁니다.**
    
    이름을 바꾸는 순간 아직 배포되지 않은 옛 코드가 전부 깨집니다. 6-3-4가 응답 필드에 대해 말하는 것과 같은 이유이고, 배포가 한 번에 끝나지 않는다는 사실은 DB 쪽에서 더 무겁습니다.
    
4.  **DB를 먼저 바꾸고 코드를 나중에 배포합니다. 바꾼 스키마가 아직 배포되지 않은 옛 코드에서도 돌아가야 합니다.**
    
    순서를 반대로 하면 새 코드가 아직 없는 컬럼을 찾습니다. 이 규칙이 2번과 3번의 이유이기도 합니다. 컬럼을 지우는 변경은 그 컬럼을 쓰는 코드가 전부 내려간 뒤 다음 배포에서 합니다.
    
5.  **되돌리는 방법을 변경마다 함께 적습니다. 되돌릴 수 없는 변경은 그렇다고 적고 따로 배포합니다.**
    
    컬럼 삭제와 타입 축소는 되돌려도 값이 돌아오지 않습니다. 다른 변경과 묶어 배포하면 그중 하나가 잘못됐을 때 묶음 전체를 되돌릴 수 없게 됩니다.
    
6.  **데이터를 옮기거나 채우는 작업은 스키마 변경과 분리하고, 여러 번 돌려도 결과가 같게 만듭니다.**
    
    행이 많으면 중간에 끊깁니다. 이어서 다시 돌릴 수 있어야 하고, 이미 처리한 행을 다시 만나도 값이 달라지지 않아야 합니다. 끊어 돌릴 수 있게 만들면 운영 중에도 나눠서 진행할 수 있습니다.
    
7.  **운영에 적용하기 전에 비슷한 규모의 데이터로 걸리는 시간을 재 봅니다.**
    
    행이 수백만인 테이블의 구조 변경은 그동안 그 테이블을 잠급니다. 개발 DB에서 1초에 끝난 변경이 운영에서 20분간 서비스를 세울 수 있고, 그 사실은 시작한 뒤에 알게 됩니다.
    

## 03\. 민감정보 저장

### 3-1 암호문 컬럼

1.  **주민등록번호, 전화번호, 이메일 같은 민감정보는 평문 컬럼으로 두지 않습니다.**
    
    덤프·백업·로그·조회 화면으로 빠져나갈 경로가 많고, 한 번 새어 나가면 회수할 수 없습니다.
    
2.  **암호문은** `_encrypted` **접미사를 붙인** `varchar` **컬럼에 저장합니다.**
    
    이름만 보고 평문이 아님을 알 수 있어야 조회·로그 코드에서 실수로 내보내지 않습니다.
    
3.  **저장하기 전에 값의 형태를 하나로 맞춥니다. 암호문과 해시를 같은 정규화된 값에서 만듭니다.**
    
    하이픈과 공백을 지웁니다. 해시 쪽은 이걸 빠뜨리면 같은 사람의 전화번호가 `010-1234-5678`과 `01012345678`로 들어와 서로 다른 해시가 되고, 중복 판정과 조회가 조용히 깨집니다. 발견했을 때는 이미 합치기 어렵습니다. 암호문 쪽은 깨지지는 않지만 같은 번호가 행마다 다른 형태로 복호화되어, 화면에 뿌릴 때와 외부로 보낼 때마다 정규화 코드가 따로 붙습니다. 하이픈을 넣어 보여주는 것은 애플리케이션의 일입니다.
    
4.  **암호문 값 안에 알고리즘과 키와 초기화 벡터를 함께 담습니다 —** `{알고리즘}.{모드}.{키}.{IV}.{암호문}`
    
    이 값 하나만 있으면 무엇으로 어떻게 잠갔는지가 전부 나와야 합니다. 그래야 알고리즘을 바꾸거나 키를 바꿀 때 기존 값을 그대로 두고 새로 저장하는 값부터 적용할 수 있습니다. 어느 하나라도 값 밖에 두면 그것을 바꾸는 순간 전체를 한꺼번에 다시 암호화해야 하고, 키 자리를 비워 두면 3-5-4의 교체 절차가 성립하지 않습니다. 키 자리에는 키 이름과 세대만 적습니다. 키 자체는 절대 들어가지 않습니다. 구분자는 마침표를 쓰고 IV와 암호문은 base64url로 패딩 없이 담습니다. base64url 알파벳에 마침표가 없어 충돌하지 않고, JWT·JWE가 쓰는 방식이라 상대가 설명 없이 알아봅니다. `$`는 셸에서 변수로 해석되어 큰따옴표 안에 넣는 순간 값이 사라집니다. 같은 형식을 API 전송에도 그대로 쓰면 경계에서 변환할 일이 없습니다.
    
    ```
    컬럼   social_number_encrypted
    저장값 aes256.gcm.ssn-k2.v5zpMhSCBhtlg5lP.-oQd-99fsXtdaBgUAMPyE_fQnVliHW_LlthFKzk
           알고리즘 모드  키     초기화 벡터       암호문 + 인증 태그
    
    조각별 길이 (주민등록번호 13자리, AES-256-GCM 기준)
      초기화 벡터  12바이트  → base64url 16글자
      암호문        13바이트 + 인증 태그 16바이트 = 29바이트 → base64url 39글자
      전체          위 예시 기준 74글자. 키 이름과 평문 길이에 따라 달라진다
    ```
    
5.  **대칭키 암호는 AES-256을 쓰고 GCM처럼 변조까지 잡아내는 모드를 먼저 고려합니다. 초기화 벡터는 값을 저장할 때마다 새로 뽑은 난수를 씁니다.**
    
    ECB 모드와 고정된 초기화 벡터는 쓰지 않습니다. 같은 평문이 항상 같은 암호문이 되어, 복호화하지 않고도 같은 값을 가진 행을 서로 찾아낼 수 있습니다. GCM을 쓰면 인증 태그가 함께 나오는데, 이것을 어디에 둘지도 형식에 포함됩니다. 128비트 태그를 암호문 뒤에 이어 붙여 마지막 조각 안에 함께 담습니다. 태그를 따로 떼어 보관하거나 버리면 변조를 잡아내지 못해 GCM을 쓰는 이유가 사라집니다. 블록 모드를 쓰기로 했다면 패딩 방식도 같은 자리에 적어 둡니다.
    
6.  **화면에 표시할 때 가리는 처리는 애플리케이션에서 하고, 가린 값을 원본 대신 저장하지 않습니다.**
    
    가린 값만 남기면 원본이 필요할 때 되돌릴 수 없고, 가림 규칙이 바뀌면 저장된 값을 전부 다시 만들어야 합니다. 원본을 담은 암호문 컬럼을 그대로 둔 채 가린 값을 담는 컬럼을 따로 더하는 것은 다른 이야기입니다 — 3-4-3.
    

### 3-2 검색용 해시

1.  **검색과 중복 판정은** `_hash` **접미사를 붙인** `char(64)` **컬럼으로 합니다.**
    
    암호문은 같은 값이라도 매번 달라 조회 조건으로 쓸 수 없습니다. 해시는 같은 값이면 같은 결과라 인덱스를 걸 수 있습니다.
    
2.  **해시는 서버가 보관하는 키를 섞은 HMAC으로 만듭니다. 키 없는 단순 해시를 쓰지 않습니다.**
    
    주민등록번호나 전화번호는 만들 수 있는 조합이 정해져 있어서, 해시 컬럼만 새어 나가도 전부 대입해 원본을 되찾을 수 있습니다.
    
3.  **해시용 키와 암호화용 키는 분리해 보관합니다.**
    
    한쪽이 노출돼도 다른 쪽이 버팁니다.
    

### 3-3 비밀번호

1.  **비밀번호는 검색용 해시와 다른 방식으로 저장합니다. bcrypt나 argon2처럼 일부러 느리게 만든 해시를 씁니다.**
    
    검색용 해시는 조회할 때마다 계산하므로 빨라야 하지만, 비밀번호는 그 빠르기가 그대로 대입 속도가 됩니다. 한 번의 계산이 오래 걸릴수록, 해시가 새어 나가도 원본을 알아내기 어려워집니다.
    
2.  **느린 해시를 쓰는 대신 로그인 시도 횟수를 제한하는 장치를 함께 둡니다.**
    
    느린 계산은 로그인할 때와 비밀번호를 바꿀 때만 들지만, 그 비용을 노린 요청이 몰리면 서버가 그대로 부담을 집니다.
    

### 3-4 복호화 지점

1.  **복호화는 서비스 계층 한 곳에서만 하고, 그 지점에서 접근 이력을 남깁니다.**
    
    여러 곳에서 복호화하면 이력을 남기지 않는 경로가 생깁니다. 누가 언제 무엇을 열람했는지 답할 수 없으면 사고가 났을 때 피해 범위를 정할 수 없습니다.
    
2.  **접근 이력은 로그가 아니라 테이블에 남깁니다.**
    
    로그는 기간이 지나면 지워집니다. 열람 이력은 나중에 조회해서 답해야 하는 자료입니다.
    
3.  **목록 조회에서 가린 값을 보여줘야 한다면, 가린 값을 저장할 때 미리 만들어 별도 컬럼에 담습니다.**
    
    암호문은 앞자리만 떼어 볼 수 없습니다. 매번 다른 IV로 암호화했기 때문에 같은 값이라도 암호문이 매번 다르고, 복호화하지 않으면 어느 자리가 무엇인지 알 수 없습니다. 그래서 목록에서 복호화 없이 가린 값을 보여주려면 저장 시점에 `010-****-5678` 같은 값을 만들어 컬럼에 함께 넣어 두는 수밖에 없습니다. 목록 한 번에 수십 건을 복호화하면 그만큼 열람 이력이 쌓이고, 실제로 그 값이 필요한 열람과 구분되지 않습니다.
    
4.  **민감정보의 보관 기간과 파기 방법을 스키마를 정할 때 함께 정합니다.**
    
    나중에 정하려 하면 어느 행이 언제 들어왔는지, 지워도 되는지를 판단할 근거가 이미 없습니다. 파기는 행 삭제가 아니라 해당 컬럼만 비우는 방식이 되는 경우가 많으므로, 비워도 나머지 업무가 돌아가는지를 미리 봐야 합니다.
    

### 3-5 키 관리

1.  **키를 코드와 저장소에 두지 않습니다. 실행 환경이 주입한 값을 읽습니다.**
    
    저장소에 한 번 들어간 값은 지워도 이력에 남습니다. 저장소를 볼 수 있는 사람 전부가 키를 볼 수 있다는 뜻이고, 외주나 인수인계로 접근 권한이 오간 이력까지 따라가야 피해 범위를 셀 수 있습니다.
    
2.  **환경마다 다른 키를 씁니다. 운영 키는 개발자 로컬에 내려오지 않습니다.**
    
    같은 키를 쓰면 개발 환경에서 새어 나간 키로 운영 데이터가 열립니다. 개발·테스트 데이터가 운영 키로 암호화되어 섞이는 문제도 함께 생깁니다.
    
3.  **키마다 이름과 세대 번호를 붙이고, 3-1-4의 키 자리에 그 이름을 적습니다.**
    
    `ssn-k2`처럼 무엇을 잠그는 키인지와 몇 번째 세대인지가 드러나게 짓습니다. 어느 키로 잠갔는지 값이 스스로 답하지 못하면, 키를 바꾸는 순간 옛 값을 열 방법이 없습니다. 이름만 적고 키 자체는 담지 않습니다.
    
4.  **키를 바꾸는 절차를 미리 적어 둡니다.**
    
    순서는 새 키를 추가하고, 새로 저장하는 값부터 새 키로 잠그고, 옛 키는 복호화 전용으로 남겨 두고, 옛 키로 잠긴 값이 없어진 뒤에 폐기하는 것입니다. 전체를 한 번에 다시 암호화하는 방식은 도중에 실패하면 어느 행이 어느 키인지 알 수 없게 되므로, 값마다 세대가 적혀 있는 3번이 전제입니다.
    
5.  **검색용 해시 키는 암호화 키와 바꾸는 방법이 다릅니다. 해시 컬럼을 하나 더 붙일 수 있게 만들어 둡니다.**
    
    암호문은 어느 키로 잠갔는지가 값 안에 적혀 있어 옛 값과 새 값이 한 컬럼에 섞여 있어도 됩니다. 해시는 조회 조건으로 쓰이기 때문에 그럴 수 없습니다. 한 컬럼에 두 세대가 섞이면 어느 쪽으로 찾아야 할지 알 수 없습니다. 그래서 새 키로 만든 해시 컬럼을 하나 더 두고, 원본을 복호화해 그 컬럼을 채우고, 다 채운 뒤 조회를 새 컬럼으로 옮기고 옛 컬럼을 지웁니다. 그동안 조회는 멈추지 않습니다. 2-10의 컬럼 추가 절차를 그대로 따르면 됩니다.
    
6.  **키를 꺼내 볼 수 있는 사람을 정하고, 그 수를 최소로 둡니다.**
    
    전부가 볼 수 있는 키는 없는 것과 같습니다. 사람이 나갈 때 무엇을 바꿔야 하는지도 이 목록이 답합니다.
    

## 04\. 코드 구조와 규약

### 4-1 디렉터리 구성

1.  **디렉터리를 업무 단위로 먼저 나눌지 계층으로 먼저 나눌지 정하고, 전체에서 한 가지만 씁니다.**
    
    두 방식이 섞이면 같은 성격의 파일이 어디 있는지 매번 확인해야 합니다. 아래 두 항이 판단 근거입니다.
    
    ```
    업무 단위 우선                   계층 우선
    src/modules/                    src/
      driver/                         controllers/
        driver.router.js                driver.controller.js
        driver.controller.js            company.controller.js
        driver.service.js             services/
        driver.repository.js            driver.service.js
      company/                          company.service.js
      settlement/                     repositories/
    ```
    
2.  **업무 단위 우선은 하나의 업무를 고칠 때 열어야 할 파일이 한 폴더에 모입니다.**
    
    업무가 늘어도 폴더 안의 파일 수는 그대로입니다. 대신 업무 폴더끼리 서로를 부르기 시작하면 의존 관계가 눈에 보이지 않게 됩니다.
    
3.  **계층 우선은 같은 계층의 코드를 나란히 놓고 비교하기 쉽습니다.**
    
    계층별 규칙을 지키는지 한눈에 보이고, 업무가 서넛뿐인 작은 서비스에서는 폴더가 덜 깊습니다. 대신 업무가 늘면 한 업무의 코드가 폴더 서너 곳에 흩어집니다.
    
4.  **두 방식은 배타적이지 않습니다. 업무 단위로 먼저 나눈 뒤 모듈 안을 다시 계층으로 나누는 프로젝트도 고려해볼 수 있습니다.**
    
    큰 축에서 전체적인 순서가 같으면 1번 규칙을 지키는 것입니다. 하위 도메인은 계층 폴더 안에서 파일 이름으로 가릅니다. 다만 한 모듈의 하위 도메인이 열 개를 넘어가면 폴더를 한 겹 더 넣기 전에 모듈 경계가 맞는지부터 봅니다. 그쯤 되면 대개 그 모듈이 사실 여러 모듈입니다.
    
    ```
    업무 → 계층 → 하위 도메인
    
    src/modules/contract-module/
      contract.router.js
      dto/  constants/
      controller/    driver.controller.js · policy.controller.js
      service/       driver.service.js · policy.service.js
      repository/    driver.repository.js · policy.repository.js
    ```
    
5.  **어느 쪽으로 정하든 여러 업무가 함께 쓰는 것은 공용 폴더 한 곳에 둡니다.**
    
    미들웨어, 암복호화, 날짜 처리, 설정 읽기가 여기 해당합니다. 업무 폴더 안에 두면 다른 업무가 그 폴더 내부를 직접 부르게 되고, 그때부터 폴더 경계가 의미를 잃습니다.
    
6.  **업무 폴더끼리는 서비스 계층을 통해서만 부릅니다.**
    
    다른 업무의 리포지토리를 직접 부르면 그 업무의 판단을 건너뛰게 됩니다. 어느 것을 고칠 때 무엇이 함께 깨지는지도 알 수 없게 됩니다.
    

### 4-2 파일과 심볼 이름

1.  **파일 이름에 계층을 붙입니다 —** `driver.service.js`**.**
    
    편집기에서 파일을 열 때 폴더 경로를 보지 않고도 무엇인지 압니다. `index.js`가 여러 개면 열린 탭만 보고는 구분되지 않습니다.
    
2.  **모듈 경로는 상대 경로 대신 별칭을 씁니다 —** `@shared/lib/crypto`**.**
    
    `../../../shared/lib/crypto`는 파일을 옮기는 순간 전부 깨지고, 몇 단계를 올라가는지 세면서 읽어야 합니다.
    
3.  **함수 이름은 무엇을 하는지로 짓고, 어떻게 하는지는 넣지 않습니다.**
    
    `findDriverByPhoneWithJoin`처럼 구현 방식이 이름에 들어가면 구현을 바꿀 때 이름이 거짓말을 하기 시작합니다.
    
4.  **조회 함수의 이름으로 결과의 개수를 구분합니다.**
    
    하나를 돌려주는 것과 목록을 돌려주는 것이 이름으로 갈리면, 부르는 쪽에서 결과를 잘못 다루는 일이 줄어듭니다. `findDriver`와 `findDrivers`처럼 단수·복수로 가르거나 접두어를 정해 씁니다.
    

### 4-3 표기와 변환 지점

하나의 값은 DB·코드·URL을 지나는 동안 서로 다른 표기로 나타납니다. `company_id`가 코드에서는 `companyId`가 되고 경로에서는 또 달라집니다. 어느 자리에서 어떤 표기를 쓰는지, 그리고 표기가 바뀌는 지점이 어디인지 두 가지가 정해져 있어야 합니다.

1.  **한 값이 자리마다 어떤 표기로 나타나는지 표로 정해 둡니다.**
    
    정해 두지 않으면 같은 값이 자리마다 다른 표기로 나타나 검색이 되지 않습니다. `companyId`로 검색했을 때 응답 코드만 나오고 DB 접근 코드가 안 나오면, 그 값을 쓰는 자리를 전부 찾았는지 확신할 수 없습니다. 표기 자체는 무엇이든 됩니다. 정해 두는 것이 규칙입니다. 아래는 한 가지 예시일 뿐이라, 요청·응답 필드를 스네이크로 채워도 규칙은 지켜집니다.
    
    ```
    같은 값 하나가 자리마다 이렇게 나타난다
    
      DB 테이블·컬럼    company_driver · company_id     스네이크
      코드 변수·속성    companyId                       카멜
      함수·메서드       findCompanyDrivers              카멜
      클래스            CompanyDriverService            파스칼
      상수              MAX_PAGE_SIZE                   대문자 스네이크
      요청·응답 필드    companyId                       카멜
      쿼리스트링        ?companyId=12                   카멜
      URL 경로          /group-substitute-driving       케밥
      폴더명            group-substitute-driving-module 케밥
      파일명            company.service.js              점으로 계층 구분
    ```
    
2.  **표기가 바뀌는 지점을 정하고, 그 밖에서는 바꾸지 않습니다.**
    
    바뀔 수 있는 자리는 둘뿐입니다. **DB와 서버 코드 사이**(리포지토리 경계), 그리고 **서버 코드와 API 응답 사이**(직렬화)입니다. 여기서 서버 코드란 컨트롤러·서비스·리포지토리가 변수와 속성에 쓰는 표기를 말합니다. 서버 코드의 표기와 API 응답 필드의 표기를 같게 맞추면 직렬화 자리에서는 바꿀 것이 없습니다. 코드가 카멜인데 응답도 카멜이면 객체를 그대로 내보내면 되니까요. 응답만 스네이크로 정했다면 직렬화 자리에서 한 번 바꾸고, 그 밖에서는 바꾸지 않습니다. 리포지토리 경계 쪽은 대개 DB 라이브러리 설정으로 전역 적용하는 편이 안전합니다. 테이블마다 손으로 매핑하면 새 컬럼을 추가할 때 빠뜨립니다. 정한 지점 밖에서 변환하면 한 곳이 빠졌을 때 오류가 나지 않고 `undefined`가 되며, 그 값은 화면에서 그냥 빈칸으로 보입니다.
    
    ```
    repository   SELECT id, company_id, created_at FROM driver …
                 ↓  여기서 한 번 바꾼다
                 { id: 3, companyId: 12, createdAt: '2026-09-06 10:00:00' }
                 ↓
    service      companyId 만 본다
    controller   companyId 만 본다
    응답          { "companyId": 12 }
    ```
    
3.  **줄임말과 두문자어의 표기를 미리 정합니다.**
    
    `id`·`url`·`api`처럼 대문자로 쓰던 말이 카멜 케이스 안에 들어올 때 사람마다 다르게 씁니다. `companyId`와 `companyID`, `apiKey`와 `APIKey`가 함께 생기면 1번의 문제가 그대로 재현됩니다. 첫 글자만 대문자로 쓰는 쪽(`companyId`, `apiKey`, `pdfUrl`)을 기본으로 삼고, 예외를 두지 않습니다.
    
4.  **주석에는 왜 그렇게 했는지를 적습니다. 무엇을 하는지는 이름으로 말합니다.**
    
    코드를 읽으면 아는 것을 주석으로 되풀이하면, 코드가 바뀌었을 때 주석이 거짓말을 하기 시작합니다. 왜 그렇게 했는지는 코드에 남지 않아 주석으로만 전달됩니다.
    

## 05\. 백엔드 계층

한 파일에 모든 로직을 쓰면 처음에는 개발이 빠릅니다. 문제는 두 번째 화면부터입니다. 같은 조회가 필요한데 그 코드가 응답을 만드는 코드와 붙어 있어 같은 서비스 로직을 부를 수 없고, 결국 코드를 복사합니다. 복사한 뒤에는 하나가 바뀔 때 두 곳을 고쳐야 하고, 한 곳을 놓치면 화면마다 다른 값이 나옵니다.

계층은 이것을 막기 위해 코드를 자리별로 나눠 두는 것입니다. 나누는 기준은 "무엇을 하는가"가 아니라 **무엇을 모르는가**입니다. 각 계층이 모르는 것이 정해져 있으면, 그 계층은 자기가 모르는 것이 바뀌어도 고칠 필요가 없습니다.

-   라우터는 **업무를 모릅니다**. 그래서 업무 규칙이 바뀌어도 라우터는 그대로입니다.
    
-   컨트롤러는 **DB를 모릅니다**. 그래서 테이블 구조가 바뀌어도 컨트롤러는 그대로입니다.
    
-   서비스는 **HTTP를 모릅니다**. 그래서 화면이 아니라 배치에서 불러도 그대로 동작합니다.
    
-   리포지토리는 **왜 부르는지 모릅니다**. 그래서 어느 업무에서 불러도 같은 결과를 냅니다.
    

계층을 건너뛰거나 섞으면 이 성질이 무너집니다. 컨트롤러에 SQL이 한 줄 들어가는 순간 컨트롤러가 테이블을 알게 되고, 그때부터 테이블 이름을 바꾸려면 컨트롤러도 찾아 고쳐야 합니다.

### 5-1 계층별 입력과 출력

| 계층 | 입력 | 출력 | 모르는 것 |
| --- | --- | --- | --- |
| router | HTTP 요청 | 컨트롤러 호출 · 분기값 주입 | 업무 규칙, 테이블 |
| controller | req, res | HTTP 응답 (상태 + 본문) | SQL, 테이블 이름 |
| dto | 요청 값 / 서비스 결과 | 검증 통과값 / 내보낼 필드 | 업무 규칙 |
| service | 값(원시 타입·객체) + db | 업무 개념의 값 | req, res, HTTP 상태 |
| repository | db + 조건 값 | 테이블 행에 가까운 값 | 업무 규칙, 호출한 이유 |

한 요청이 계층을 지나며 값이 어떻게 바뀌는지가 아래입니다. 어느 계층에 무엇을 둘지 헷갈리면 이 흐름의 어느 칸에 해당하는지를 봅니다.

```
GET /api/group-substitute-driving/drivers?companyId=12&page=1

router        경로를 확인하고 인증 미들웨어를 태운다
              경로가 정하는 분기값(대상 DB 등)을 넘길 준비를 한다
    │  req, res
    ▼
controller    요청 dto 로 쿼리스트링을 검증한다
              → { companyId: 12, page: 1, size: 20 }
    │  ({ companyId: 12, page: 1, size: 20 }, db, user)
    ▼
service       업무 판단 — 이 사용자가 이 업체를 볼 수 있는가
              page 를 offset 으로 바꾼다
    │  (db, { companyId: 12, offset: 0, limit: 20 })
    ▼
repository    SELECT id, name, phone_encrypted … WHERE company_id = ?
    │  [ { id: 3, name: '홍길동', phoneEncrypted: 'aes256.gcm.…' }, … ]
    ▲
service       전화번호를 복호화해 가린 값으로 바꾼다
    │  [ { id: 3, name: '홍길동', phone: '010-****-5678' }, … ]
    ▲
controller    응답 dto 로 내보낼 필드를 고르고 상태 코드를 정한다
    │  200 { data: [ … ], pagination: { page: 1, size: 20, total: 137 } }
    ▲
```

### 5-2 router

1.  **라우터에는 경로 정의와 미들웨어 배치만 둡니다.**
    
    라우터에 조건문이 생겼다면 그것은 컨트롤러의 일입니다. 라우터가 얇으면 그 파일 하나가 모듈의 목차 역할을 해서, 이 모듈이 무슨 경로를 갖고 있고 어디에 인증이 걸려 있는지 한눈에 보입니다.
    
2.  **요청마다 달라지는 분기값은 라우터에서 정해 아래로 넘깁니다 — 대상 DB, 대상 환경 같은 것.**
    
    경로 파라미터가 그 값을 갖고 있으니 판단도 라우터에 둡니다. 아래 계층이 스스로 고르면 같은 함수를 다른 대상에 쓸 수 없고, 대상이 하나 늘 때마다 그 함수들을 전부 찾아 고쳐야 합니다. 다만 DB가 하나뿐인 시스템은 여기 해당하지 않습니다. 커넥션은 시작할 때 한 번 잡아 두고 리포지토리가 그대로 쓰면 됩니다. 그리고 배치와 스케줄러에는 요청이 없으므로, 그쪽이 같은 값을 어디서 얻을지도 함께 정해 둡니다.
    
3.  **인증 미들웨어는 라우터에서 걸고, 새 경로는 기본적으로 인증 대상에 넣습니다. 인증이 필요 없는 경로만 따로 모아 인증 미들웨어보다 앞에 등록합니다.**
    
    미들웨어는 등록 순서대로 걸리므로, 순서를 잘못 두면 인증을 걸려던 경로가 공개됩니다. 기본을 인증 대상으로 두면 실수했을 때 공개되는 대신 인증이 걸립니다. 막혀서 못 쓰는 것은 바로 알려지지만, 열려 있는 것은 아무도 알려주지 않습니다.
    

### 5-3 controller

1.  **컨트롤러는 두 가지만 합니다 — "제대로 들어왔나"와 "어떻게 돌려줄까".**
    
    앞은 요청 DTO 검증이고 뒤는 응답 규격과 상태 코드입니다. 이 둘 밖의 일이 컨트롤러에 있으면 그것은 다른 계층의 일입니다.
    
2.  **컨트롤러는 테이블 이름과 SQL을 모릅니다.**
    
    컨트롤러가 테이블을 알게 되면 스키마를 고칠 때 고쳐야 할 파일이 리포지토리 밖으로 번집니다.
    
3.  **서비스에서 받은 결과를 응답 규격으로 옮기기만 하고, 값을 더하거나 조건을 붙이지 않습니다.**
    
    여기서 값을 만들기 시작하면 같은 업무를 다른 경로나 배치에서 부를 때 그 처리가 빠집니다. 화면에 필요한 값이 모자라면 컨트롤러에서 채우지 말고 서비스가 내주게 합니다.
    
4.  **표시 가공은 컨트롤러가 하고, 노출 범위를 줄이는 것은 서비스가 합니다.**
    
    날짜 포맷을 바꾸거나 하이픈을 넣는 것은 컨트롤러입니다. 민감정보를 가리는 것은 서비스입니다. 둘 다 보여주는 모양처럼 보이지만 뒤쪽은 보안 경계를 넘습니다. 컨트롤러가 가리려면 평문을 받아야 하는데, 평문이 서비스 밖으로 나오는 순간 행을 통째로 내보내는 코드 한 줄이면 유출입니다. 서비스가 가려서 주면 컨트롤러는 평문을 쥔 적이 없어 실수로도 흘릴 수 없습니다. 평문을 정말 내보내야 하는 응답이라면 서비스가 평문을 주고 응답 DTO에 그 필드를 선언합니다. 그 선언 자체가 이 응답은 평문을 내보낸다는 기록이 됩니다.
    
5.  **오류는 직접 응답으로 만들지 않고 다음으로 넘깁니다.**
    
    컨트롤러마다 오류를 붙잡아 응답을 만들면, 그 컨트롤러를 쓰지 않는 경로에서 같은 오류가 다르게 나갑니다. 넘기기만 하면 처리 방식은 5-4가 한 곳에서 정합니다.
    
6.  **인증된 사용자 정보와 라우터가 정한 분기값을 서비스에 인자로 넘깁니다.**
    
    서비스가 요청 객체에서 직접 꺼내면 서비스가 HTTP를 알게 됩니다. 필요한 값만 뽑아 넘기면 배치에서 같은 서비스를 그대로 부를 수 있습니다.
    

### 5-4 dto

1.  **DTO는 통신 규격입니다. 요청 DTO는 들어오는 것을 막는 관문이고, 응답 DTO는 나가는 것을 고르는 관문입니다.**
    
    두 방향 모두 "선언되지 않은 것은 통과하지 않는다"가 핵심입니다. 이 관문이 없으면 요청 본문이 그대로 저장에 흘러가고, 조회한 행이 그대로 응답에 실립니다.
    
2.  **DTO는 형식만 봅니다. 업무 규칙은 보지 않습니다.**
    
    이 구분이 가장 헷갈리는 자리입니다. "값이 숫자인가", "필수인가", "날짜 형식이 맞는가"는 DTO입니다. "이 업체가 이 증권을 쓸 수 있는가", "이미 신청한 건인가"는 서비스입니다. 판단 기준은 **DB를 봐야 아는가**입니다. 봐야 알면 DTO가 아닙니다.
    
3.  **요청 DTO에 선언되지 않은 필드는 버립니다.**
    
    화면이 보내지 않기로 한 필드를 직접 넣어 보내는 것만으로 값이 바뀌는 것을 막습니다.
    
4.  **URL 파라미터와 쿼리스트링도 DTO로 검증합니다.**
    
    본문만 검증하고 넘어가는 경우가 많습니다. 경로의 `:id`에 숫자가 아닌 값이 들어오면 그대로 질의문까지 내려갑니다.
    
5.  **응답 DTO에 선언되지 않은 값은 내보내지 않습니다.**
    
    비밀번호 해시와 암호문 컬럼이 여기서 걸러집니다. 조회한 행을 통째로 돌려주는 코드가 한 곳만 있어도 그 값들이 밖으로 나갑니다. 다만 화면에 필요한 값이 선언에서 빠지면 조용히 사라지므로, 필드를 추가할 때 함께 확인합니다.
    
6.  **응답 DTO는 모듈 안에서 재사용하고, 화면마다 새로 만들지 않습니다.**
    
    같은 자원을 두 화면이 다른 DTO로 내보내면 필드 이름과 형식이 조금씩 갈립니다. 화면마다 필요한 필드가 다르면 DTO를 나누는 대신 내보낼 필드 목록을 인자로 받습니다.
    

### 5-5 service

1.  **서비스는 HTTP를 모릅니다.** `req`**·**`res`**를 넘기지 않습니다.**
    
    서비스가 HTTP를 알면 배치나 스케줄러에서 같은 업무를 부를 수 없습니다. 문자 발송 배치가 화면과 같은 판단을 해야 하는 경우가 반드시 생깁니다.
    
2.  **업무 판단은 전부 서비스에 있습니다.**
    
    "무엇이 유효한 상태인가", "이 값을 어떻게 계산하는가", "이 사람이 이 건을 볼 수 있는가"가 여기 해당합니다. 같은 판단이 두 곳에 있으면 한쪽만 고치는 일이 생기고, 같은 데이터인데 화면마다 다른 값이 나옵니다.
    
3.  **서비스는 SQL을 조립하지 않습니다.**
    
    조건이 하나 더 필요하면 리포지토리에 함수를 추가합니다. 서비스에 질의가 들어오기 시작하면 리포지토리는 껍데기만 남고, 어떤 질의가 나가는지 세는 것부터 되지 않습니다.
    
4.  **서비스가 다른 서비스를 부르는 것은 됩니다. 다만 트랜잭션을 넘기는 방법을 정해 둡니다.**
    
    같은 트랜잭션 안에서 다른 업무를 불러야 할 때, 트랜잭션 객체를 인자로 받을지 아니면 부르는 쪽이 전부 묶을지 한 가지로 정합니다. 정하지 않으면 안쪽 서비스가 별도 트랜잭션을 열어 바깥이 실패해도 되돌아가지 않습니다.
    
5.  **값을 만들어 내는 계산은 함수 하나로 만들어 한 번만 둡니다.**
    
    나이 계산, 보험료 계산, 기간 판정처럼 결과가 화면에 그대로 보이는 것들입니다. 여러 서비스가 함께 쓰면 공용 모듈로 올립니다.
    
6.  **계산해서 만든 값도 테이블에서 읽어 온 행과 같은 형태로 돌려줍니다.**
    
    나중에 저장을 계산으로 바꾸거나 그 반대로 바꿔도, 불러 쓰는 코드를 고치지 않아도 됩니다. 1-1-3에서 "판단이 서지 않으면 저장하지 않는 쪽"을 고를 수 있는 것도 이 규칙이 받쳐 주기 때문입니다.
    

### 5-6 repository

1.  **질의 하나에 함수 하나입니다.**
    
    함수 하나가 조건에 따라 다른 질의를 만들기 시작하면, 그 안에 업무 판단이 들어온 것입니다. 실행 계획을 확인할 때도 어떤 질의가 나가는지 알 수 없습니다.
    
2.  **모든 함수는 첫 인자로** `db`**를 받고, 스스로 커넥션을 고르지 않습니다.**
    
    커넥션을 안에서 고르면 트랜잭션 안에서 부를 수 없습니다. 트랜잭션은 커넥션을 잡고 있는 객체이고, 그것을 받아 써야 같은 트랜잭션에 들어갑니다.
    
3.  **리포지토리는 왜 부르는지 모릅니다. 업무 판단을 하지 않습니다.**
    
    "유효한 것만 조회"처럼 들리는 조건도 대부분 업무 판단입니다. 무엇이 유효한지가 업무 규칙이기 때문입니다. 조건 값을 인자로 받아 그대로 거는 것이 리포지토리의 일입니다.
    
4.  **테이블 행에 가까운 값을 돌려주고, 화면에 맞춘 가공은 하지 않습니다.**
    
    화면에 맞춰 가공한 결과를 돌려주면 화면이 하나 늘 때마다 리포지토리 함수가 하나 늘고, 같은 조회가 여러 벌 생깁니다.
    
5.  **함수 이름으로 결과가 하나인지 목록인지 구분합니다.**
    
    `findDriver`와 `findDrivers`처럼 단수·복수로 가르거나 접두어를 정해 씁니다. 부르는 쪽에서 결과를 잘못 다루는 일이 줄어듭니다.
    
6.  `SELECT *`**를 쓰지 않고 필요한 컬럼만 적습니다.**
    
    컬럼이 추가되면 그 값이 응답까지 그대로 흘러갈 수 있고, 암호문 컬럼이 여기 섞여 나갑니다. 어떤 컬럼을 쓰는지가 코드에 적혀 있어야 스키마를 고칠 때 영향 범위를 셀 수 있습니다.
    
7.  **목록을 가져온 뒤 행마다 다시 조회하지 않습니다.**
    
    20건을 조회하고 각 건의 소속을 하나씩 조회하면 질의가 21번 나갑니다. 조인하거나 식별자를 모아 한 번에 조회합니다.
    

### 5-7 계층 경계

1.  **업무를 처리하는 경로는 계층을 모두 채웁니다. 간단하다는 이유로 계층을 건너뛰지 않습니다.**
    
    건너뛰면 업무 판단이 컨트롤러나 질의문에 섞여 들어가고, 다음에 같은 판단이 필요한 사람은 그것을 찾지 못해 다시 씁니다. 다만 헬스 체크처럼 업무 판단도 DB 접근도 없는 경로는 라우터에서 바로 응답해도 됩니다.
    
2.  **한 계층은 바로 아래 계층만 부릅니다. 컨트롤러가 리포지토리를 직접 부르지 않습니다.**
    
    건너뛴 호출이 하나 생기면 그 뒤로 계속 늘어납니다. 조회만 하니까 괜찮다고 생각한 자리에 나중에 업무 판단이 붙습니다.
    
3.  **아래 계층이 위 계층을 부르지 않습니다.**
    
    리포지토리가 서비스를 부르기 시작하면 호출이 순환하고, 어느 쪽부터 읽어야 하는지 정해지지 않습니다.
    
4.  **파일이 하나뿐이어도 계층 폴더를 유지합니다.**
    
    지금 짧다고 폴더를 합치면, 커졌을 때 쪼개는 일이 파일을 옮기고 참조를 전부 고치는 일이 됩니다. 처음부터 자리가 나뉘어 있으면 파일만 늘리면 됩니다.
    
5.  **기능이 늘면 계층 폴더 안에서 관심사별로 파일을 쪼갭니다.**
    
    `controller/policy.controller.js`, `controller/driver.controller.js`처럼 나눕니다. 한 파일이 커지는 것을 계층을 늘려 해결하지 않습니다.
    
6.  **새 모듈은 아래에서 위로 만듭니다 — 리포지토리 → 서비스 → DTO → 컨트롤러 → 라우터.**
    
    위에서 시작하면 아래 계층이 무엇을 돌려줄지 정해지지 않은 채로 응답 모양을 먼저 정하게 되고, 결국 컨트롤러에서 값을 맞추게 됩니다.
    

### 5-8 트랜잭션

1.  **트랜잭션은 서비스 계층에서만 시작하고 끝냅니다.**
    
    리포지토리가 각자 트랜잭션을 열면, 여러 테이블을 함께 바꾸는 작업이 중간까지만 저장된 채로 끝날 수 있습니다.
    
2.  **트랜잭션 안에서 외부 시스템을 부르지 않습니다.**
    
    외부 호출은 느리고 언제 끝날지 모릅니다. 그동안 잠금이 유지되어 다른 요청이 밀립니다. 외부 호출이 실패해도 트랜잭션은 되돌릴 수 있지만, 성공한 외부 호출은 되돌릴 수 없다는 점도 함께 고려해야 합니다.
    
3.  **트랜잭션은 필요한 만큼만 감싸고 조회만 하는 코드는 넣지 않습니다.**
    
    범위가 넓을수록 잠금이 오래 유지되고 교착이 생길 확률이 올라갑니다.
    
4.  **여러 요청이 같은 행을 동시에 바꿀 수 있는 업무는 그 사실을 먼저 인정하고 막을 방법을 정합니다.**
    
    재고 차감, 순번 발급, 잔액 갱신, 중복 신청이 여기 해당합니다. 조회한 값을 그대로 계산해 저장하는 코드는 두 요청이 겹치면 한쪽 결과가 사라집니다. 화면에서 두 번 눌리는 것만으로도 재현됩니다.
    
5.  **같은 요청이 두 번 들어와도 결과가 한 번과 같아야 하는 API는 그렇게 만듭니다.**
    
    응답이 늦으면 사용자는 다시 누르고, 네트워크는 같은 요청을 두 번 보냅니다. 요청마다 식별자를 받아 이미 처리한 것인지 확인하거나, 유니크 제약으로 두 번째를 막습니다.
    

### 5-9 외부 시스템 호출

1.  **외부 시스템을 부르는 코드는 별도 모듈로 감싸고, 서비스는 그 모듈만 부릅니다.**
    
    외부 API의 응답 형태가 서비스 코드 안까지 들어오면, 상대가 규격을 바꿀 때 고칠 자리가 여러 곳이 됩니다.
    
2.  **벤더가 바뀔 수 있는 것은 구현을 갈아 끼울 수 있게 나눕니다 — 문자, 메일, 파일 저장소.**
    
    부르는 쪽은 `send(번호, 내용)`만 알고, 어느 벤더로 나가는지는 설정이 정합니다. 벤더를 바꿀 때 부르는 코드를 고치지 않아도 되고, 로컬에서는 실제로 나가지 않는 구현으로 바꿔 끼울 수 있습니다.
    
3.  **모든 외부 호출에 시간 제한을 겁니다.**
    
    제한이 없으면 상대가 응답하지 않을 때 우리 쪽 요청도 함께 멈춰 있습니다. 그런 요청이 쌓이면 커넥션이 동나고 서비스 전체가 멈춥니다.
    
4.  **재시도할 실패와 하지 말 실패를 구분합니다.**
    
    연결이 끊긴 것은 다시 걸면 되지만, 상대가 "잘못된 요청"이라고 답한 것은 몇 번을 걸어도 같습니다. 구분하지 않고 재시도하면 상대 시스템에 같은 잘못된 요청을 계속 보냅니다.
    
5.  **응답을 기다릴 필요가 없는 일은 요청 처리에서 떼어 냅니다 — 문자·메일 발송, 파일 생성, 외부 전송.**
    
    요청 처리 안에서 하면 사용자가 그 시간만큼 기다리고, 발송이 실패하면 저장까지 함께 실패합니다.
    
6.  **떼어 낸 일은 큐나 작업 테이블에 넣고, 상태와 시도 횟수와 마지막 실패 이유를 남깁니다.**
    
    같은 프로세스에서 나중에 처리하는 방식은 그 프로세스가 재시작되면 남은 일이 사라집니다. 실패한 작업이 몇 건이고 왜 실패했는지 물어볼 곳도 필요합니다.
    

### 5-10 스케줄러

1.  **스케줄러는 자기 안에 업무 로직을 두지 않고 서비스를 부릅니다.**
    
    화면에서 하는 일과 배치가 하는 일이 같은 판단을 써야 합니다. 스케줄러가 따로 계산하면 화면과 배치가 다른 값을 내놓습니다.
    
2.  **주기 실행 작업은 겹쳐 도는 것을 막습니다.**
    
    앞선 실행이 아직 끝나지 않았는데 다음 실행이 시작되면 같은 대상을 두 번 처리합니다. 실행 중임을 표시하고 시작 전에 확인하거나, 처리 대상을 집을 때 잠금을 겁니다.
    
3.  **스케줄러를 켤지 말지를 설정으로 가릅니다.**
    
    설정 없이 앱과 함께 무조건 뜨면, 로컬에서 서버만 띄워도 운영 데이터를 폴링하고 문자나 메일이 실제로 나갑니다. 개발자가 코드를 주석 처리해 가며 막게 되고, 그 주석이 그대로 커밋됩니다.
    
4.  **실행 주기는 코드에 박지 않고 설정에서 읽습니다.**
    
    운영 중에 주기를 바꿔야 하는 일이 생깁니다. 코드에 박혀 있으면 배포해야 바뀝니다.
    

## 06\. API 규약

### 6-1 경로와 메서드

1.  **경로는 자원 이름으로 만들고, 조회·등록·수정·삭제는 HTTP 메서드로 가릅니다.**
    
    화면마다 전용 경로를 만들면 같은 자원을 조회하는 경로가 여럿 생깁니다. 그 자원의 조회 조건이나 반환 항목을 하나 고칠 때마다 경로 수만큼 같은 수정을 반복하게 됩니다.
    
2.  **자원을 만들고 지우는 것으로 표현되지 않는 업무 행위는, 그 행위 이름을 그대로 쓴 경로로 둡니다 —** `POST /orders/{id}/cancel`**.**
    
    승인·취소·확정·반려가 여기 해당합니다. 억지로 수정으로 표현하면 무엇이 바뀌는 수정인지 본문을 열어 봐야 알 수 있고, 권한도 따로 걸 수 없습니다.
    
3.  **경로에 동사나 화면 이름을 넣지 않습니다 —** `/getDriverList`**,** `/driverPage`**.**
    
    화면이 바뀌면 경로가 남습니다. 화면 이름으로 만든 경로는 화면을 없앤 뒤에도 지워도 되는지 아무도 모릅니다.
    
4.  **경로를 합칠지는 세 가지로 판단합니다 — 반환하는 행의 단위가 같은가, 인증 주체가 같은가, 차이가 파라미터로 표현되는가.**
    
    셋 중 하나라도 아니면 사실 다른 자원이고, 억지로 합치면 조건 분기가 경로 안에 쌓입니다. 특히 인증 주체가 다르면 같은 자원이라도 열어 줄 필드 범위가 달라, 합쳤다가 조건 하나가 빠지면 그대로 노출됩니다.
    
5.  **목록 조회의 페이지 나눔·정렬·검색 조건 파라미터 이름을 프로젝트 전체에서 하나로 고정합니다.**
    
    경로마다 다르면 화면 코드가 경로마다 다른 형태를 만들어야 하고, 공통 목록 컴포넌트를 쓸 수 없습니다.
    

### 6-2 요청 검증

1.  **날짜·시각·금액처럼 형식이 갈리는 값은 주고받는 형식을 정해 둡니다.**
    
    같은 날짜를 어떤 화면은 `2026-09-06`으로, 어떤 화면은 `20260906`으로 보내기 시작하면 서버가 양쪽을 다 받아 주게 되고, 그때부터 어느 쪽이 규격인지 사라집니다.
    
2.  **시각에는 시간대를 붙여 주고받습니다. 붙이지 않기로 했다면 어느 시간대의 값인지를 명세에 못 박습니다.**
    
    `2026-09-06 14:22:10`만 받은 쪽은 이것이 어느 시간대의 14시인지 알 방법이 없습니다. 양쪽이 같은 나라에 있는 동안은 드러나지 않다가, 상대 서버가 UTC로 도는 환경으로 옮겨 간 날부터 아홉 시간씩 어긋난 값이 들어옵니다. `2026-09-06T14:22:10+09:00`처럼 오프셋을 붙이면 값이 스스로 답을 갖고 있어 그럴 일이 없고, 상대가 어느 언어를 쓰든 표준 함수로 그대로 읽습니다. 국내 전용 연동이라 오프셋 없이 가기로 했다면 명세 앞쪽에 시간대를 적고 모든 예시를 그 형식으로 통일합니다. 2-4-7이 DB와 서버 안쪽의 시간대를 맞추는 규칙이라면, 이것은 그 값이 밖으로 나갈 때의 규칙입니다.
    
3.  **목록 조회에 개수 상한을 둡니다.**
    
    상한이 없으면 전체를 요청하는 호출 하나가 서버와 DB를 함께 세웁니다. 화면이 요청한 개수를 그대로 믿지 않고 상한으로 자릅니다.
    
4.  **질의문은 파라미터 바인딩으로만 만듭니다. 값을 문자열로 이어 붙이지 않습니다.**
    
    이어 붙이는 방식은 값 안에 질의문을 넣어 보내는 것을 막지 못합니다. 한 곳만 있어도 DB 전체가 열립니다.
    

### 6-3 응답

1.  **성공과 실패 응답의 형태를 프로젝트 전체에서 하나로 고정합니다.**
    
    예외가 하나 생기면 그 예외를 아는 사람만 그 경로를 쓸 수 있게 됩니다.
    
2.  **성공했는지를 본문에 다시 담지 않습니다. 그 답의 정본은 HTTP 상태 코드입니다.**
    
    `success` 같은 결과 표시 필드를 두면 같은 사실이 두 곳에 저장된 것이 되고, 1장이 말하는 대로 언젠가 갈립니다. 갈리는 방향도 정해져 있습니다. `200`에 `success: false`를 담는 경로가 생기고, 그때부터 게이트웨이·캐시·모니터링·오류 추적 도구가 그 요청을 전부 성공으로 셉니다. 장애가 나도 실패율 그래프가 움직이지 않아 아무도 모릅니다. 성공 응답에는 요청한 자원만 담습니다.
    
3.  **실패 응답의 형태는 RFC 9457을 따르고,** `Content-Type`**은** `application/problem+json`**으로 내보냅니다.**
    
    오류 본문 규격을 새로 만들 이유가 없습니다. 표준 형식이면 상대가 설명을 읽지 않아도 어느 필드가 무엇인지 알고, 만들어져 있는 클라이언트 라이브러리가 그대로 해석합니다. 필드는 `type`(오류 종류를 가리키는 URI, 없으면 `about:blank`), `title`(사람이 읽는 짧은 이름), `status`(HTTP 상태와 같은 값), `detail`(이번 경우에 대한 설명), `instance`(문제가 난 자원의 경로)입니다. 필요한 필드를 더 붙일 수 있게 되어 있으므로 업무 오류 코드는 `code`로 더하고, 필드별 검증 실패처럼 여러 건을 알려야 하면 `errors` 배열을 더합니다. 화면은 `code`로 분기합니다. 메시지 문자열을 비교해 분기하면 문구를 다듬는 순간 분기가 깨집니다.
    
    ```
    409 Conflict
    Content-Type: application/problem+json
    
    {
      "type": "https://api.example.com/problems/duplicated-driver",
      "title": "이미 등록된 기사",
      "status": 409,
      "detail": "같은 업체에 같은 주민등록번호가 이미 등록되어 있습니다",
      "instance": "/api/external/drivers",
      "code": "DUPLICATED_DRIVER"
    }
    ```
    
4.  **목록 응답에는 전체 건수와 현재 위치를 함께 담습니다.**
    
    화면이 페이지 번호를 그리려면 전체 건수가 필요합니다. 따로 부르게 하면 두 번의 호출 사이에 데이터가 바뀌어 개수와 목록이 어긋납니다. 배열만 내보내면 이 값들을 담을 자리가 없으므로 목록은 객체로 감쌉니다. 이때 감싸는 필드는 결과를 담는 자리이지 성공을 알리는 표시가 아닙니다.
    
5.  **이미 쓰이고 있는 응답 필드는 이름을 바꾸거나 없애지 않습니다.**
    
    화면과 서버는 동시에 배포되지 않습니다. 필요하면 새 필드를 추가해 함께 내려보내다가 옛 필드를 뗍니다.
    
6.  **API 명세는 코드에서 만들어 냅니다.**
    
    손으로 쓴 문서는 나중에 실제와 갈립니다. 코드와 DTO가 곧 문서입니다.
    

### 6-4 오류 처리

1.  **예상한 실패와 예상하지 못한 실패를 다른 방식으로 다룹니다.**
    
    "이미 신청한 건입니다"는 업무 규칙이고 사용자에게 그대로 보여줘야 합니다. 데이터베이스 연결이 끊어진 것은 사용자가 할 수 있는 일이 없고, 내부 내용을 보여줘도 안 됩니다. 둘을 같은 방식으로 다루면 내부 오류 메시지가 화면에 나가거나, 업무 안내가 "오류가 발생했습니다"로 뭉개집니다.
    
2.  **업무 규칙 위반은 전용 오류 형태로 만들어 던지고, 컨트롤러가 아니라 공통 처리기에서 응답으로 바꿉니다.**
    
    경로마다 오류를 응답으로 바꾸면 형태가 조금씩 달라집니다. 한 곳에서 바꾸면 응답 규격이 저절로 지켜집니다.
    
3.  **예상하지 못한 실패는 전체를 잡아내는 처리기를 마지막에 두고, 여기서 로그를 남기고 일반적인 응답을 내려줍니다.**
    
    처리기가 없으면 스택 트레이스가 그대로 응답에 실려 나가고, 그 안에 파일 경로와 쿼리가 들어 있습니다.
    
4.  **오류를 삼키지 않습니다. 붙잡았으면 다루거나 다시 던집니다.**
    
    붙잡아 놓고 아무것도 하지 않은 자리는 나중에 문제가 생겼을 때 아무 흔적도 남기지 않습니다. 원인을 찾는 데 가장 오래 걸리는 종류의 결함이 여기서 나옵니다.
    
5.  **오류 로그에는 무엇이 잘못됐는지에 더해 그때 어떤 값이었는지를 남깁니다.**
    
    오류 메시지만 있으면 재현을 못 합니다. 요청 식별자를 남겨 그 요청의 다른 줄과 이어 붙이고(6-6-1), 판단에 쓰인 값을 함께 적습니다. 요청 본문은 6-6-3에 따라 가려서 남깁니다.
    

### 6-5 인증과 인가

1.  **인증 주체가 다르면 다른 미들웨어를 타게 합니다.**
    
    내부 사용자, 고객사 사용자, 링크만 들고 오는 외부 사용자는 확인해야 할 것과 열어 줄 범위가 모두 다릅니다.
    
2.  **인증과 인가를 나눠서 겁니다. 인증은 미들웨어에서, "이 사람이 이 건을 볼 수 있는가"는 서비스 계층에서 확인합니다.**
    
    로그인했다는 사실만으로는 남의 건을 열어도 되는지 알 수 없습니다. 경로에 붙은 식별자가 그 사람의 것인지는 데이터를 읽어 봐야 알 수 있고, 그것은 업무 판단이라 서비스 계층의 일입니다. 미들웨어에서만 막으면 `/orders/1234`의 숫자를 바꿔 남의 주문을 여는 문제가 그대로 남습니다.
    
3.  **조회 조건에 소속을 항상 함께 넣습니다.**
    
    식별자로만 조회하면 그 행이 다른 소속의 것이어도 결과가 나옵니다. 조회 단계에서 소속을 함께 걸면 인가 확인을 빠뜨려도 애초에 행이 나오지 않습니다.
    
4.  **처리자 값은 인증된 세션에서 가져옵니다. 화면에서 넘어온 값을 쓰지 않습니다.**
    
    화면에서 넘어온 값을 그대로 저장하면 기록을 조작할 수 있습니다.
    
5.  **관리자 기능은 경로를 숨기는 것이 아니라 권한으로 막습니다.**
    
    경로를 모를 것이라는 가정은 화면 코드가 브라우저에 내려가는 순간 깨집니다.
    

### 6-6 로그

1.  **요청마다 식별자를 하나 붙이고, 그 요청이 남기는 모든 줄에 함께 적습니다.**
    
    식별자가 없으면 여러 요청이 동시에 남긴 줄이 섞여, 하나의 요청이 무엇을 했는지 이어서 읽을 수 없습니다. 앞단에서 이미 붙여 보낸 식별자가 있으면 그것을 그대로 이어받고, 외부 시스템을 부를 때도 함께 넘깁니다. 장애가 났을 때 상대 쪽 로그와 맞춰 볼 수 있는 유일한 열쇠입니다.
    
2.  **요청과 응답을 남깁니다 — 시각, 요청 식별자, 메서드, 경로, 상태 코드, 처리 시간, 요청 헤더와 본문, 응답 헤더와 본문.**
    
    이것을 남기지 않으면 "우리는 분명히 보냈다"와 "우리는 받은 적 없다"가 맞설 때 답할 방법이 없습니다. 외부 연동에서는 이 기록이 있느냐 없느냐가 책임 소재를 가릅니다. 화면에서 무엇을 눌렀길래 그 값이 저장됐는지도 요청 본문이 있어야 답합니다. 처리 시간을 함께 남기면 느려진 경로를 따로 재는 장치 없이도 찾아냅니다.
    
3.  **남기기 전에 민감정보를 가립니다. 가릴 필드 목록은 코드 한 곳에 두고 거기서만 고칩니다.**
    
    비밀번호, 주민등록번호, 전화번호, 카드번호, 그리고 `authorization`·`cookie`·`x-api-key` 같은 인증 헤더입니다. 목록이 여러 곳에 흩어지면 새 필드가 한쪽에만 추가되고, 빠진 쪽으로 평문이 그대로 남습니다. 로그는 3장이 막아 둔 것을 가장 쉽게 우회하는 경로입니다. 암호화해 저장한 값이 로그에는 평문으로 남아 있는 일이 실제로 자주 일어납니다.
    
4.  **본문이 크면 잘라서 남기고, 파일 본문은 남기지 않습니다.**
    
    길이 상한을 정해 두고 넘으면 잘린 것을 표시합니다. 업로드 본문을 그대로 남기면 로그가 며칠 만에 디스크를 채우고, 그때는 로그를 지우는 것 말고 할 수 있는 일이 없습니다.
    
5.  **한 줄에 하나의 사건을, 기계가 읽을 수 있는 형태로 남깁니다.**
    
    문장으로 이어 붙인 로그는 사람이 눈으로 읽을 때만 쓸모가 있습니다. 필드로 나눠 두면 요청 식별자나 상태 코드로 걸러 볼 수 있고, 여러 줄에 걸친 오류 기록도 하나로 묶입니다. 여러 대로 나눠 도는 서버에서는 이것이 없으면 로그를 모아 놓아도 볼 수가 없습니다.
    
6.  **로그의 보관 기간을 정합니다. 열람 이력과 같은 자리에 두지 않습니다.**
    
    로그는 기간이 지나면 지워지는 것이 정상입니다. 나중에 조회해서 답해야 하는 자료는 3-4-2에 따라 테이블에 남깁니다. 둘을 섞으면 지워도 되는 것을 못 지우거나, 남겨야 할 것이 함께 사라집니다.
    

## 07\. 테스트

테스트하기 어려우면 대개 계층이 섞인 것입니다. 서비스가 HTTP를 모르면 요청 객체를 지어낼 필요가 없고, 리포지토리가 커넥션을 인자로 받으면 시험용 트랜잭션에 묶어 돌릴 수 있습니다. 5장을 지킨 대가가 여기서 돌아옵니다.

### 7-1 무엇을 테스트하는가

1.  **값을 만들어 내는 계산과 업무 판단에는 반드시 테스트를 붙입니다.**
    
    나이 계산, 보험료 계산, 기간 판정, 유효 상태 판정 같은 것입니다. 여기가 틀리면 어디에서도 오류가 나지 않고 숫자만 조용히 틀립니다. 반대로 여기만 지켜지면 화면이 바뀌어도 값은 유지됩니다.
    
2.  **리포지토리는 실제 DB에 붙여 확인합니다. 질의를 흉내 낸 가짜로 대신하지 않습니다.**
    
    확인하려는 것이 질의가 맞는가인데, 가짜로 바꾸면 그 질의는 한 번도 실행되지 않습니다. 컬럼 이름 오타나 조인 조건 실수가 그대로 통과합니다.
    
3.  **컨트롤러는 따로 떼어 시험하지 않고, 요청부터 응답까지 한 줄로 훑습니다.**
    
    컨트롤러만 떼면 아래 계층을 전부 가짜로 채우게 되고, 남는 것은 가짜가 시킨 대로 동작하는지 확인하는 일뿐입니다. 경로 하나를 실제로 호출해 상태 코드와 응답 모양을 봅니다.
    
4.  **결함을 고칠 때는 그 결함을 재현하는 테스트를 먼저 만듭니다.**
    
    재현하지 못한 채 고치면 고쳐졌는지 확신할 수 없고, 같은 결함이 다시 들어오는 것도 막지 못합니다.
    
5.  **커버리지 숫자를 목표로 삼지 않습니다.**
    
    숫자를 맞추려 들면 값을 확인하지 않고 함수만 한 번씩 불러 보는 테스트가 생깁니다. 숫자 대신 1번에 적은 것들에 테스트가 있는지를 봅니다.
    

### 7-2 어디에 두는가

1.  **단위 테스트는 대상 파일 옆에 같은 이름으로 둡니다 —** `driver.service.test.js`**.**
    
    모듈을 옮길 때 테스트가 따라옵니다. 테스트가 없는 파일이 폴더에서 바로 눈에 띕니다.
    
2.  **여러 모듈에 걸치거나 서버를 띄워야 하는 테스트는 별도 폴더에 모읍니다.**
    
    어느 한 모듈의 것이 아니라 옆에 둘 자리가 없습니다. 실행 시간도 길어서 따로 돌릴 수 있어야 합니다.
    
    ```
    src/modules/driver/
      driver.service.js
      driver.service.test.js       단위
      driver.repository.js
      driver.repository.test.js    실제 DB 에 붙는다
    test/
      driver.api.test.js           서버를 띄워 HTTP 로 부른다
      helpers/
        db.js                      스키마 준비와 정리
        fixture.js                 시험 데이터 만들기
    ```
    
3.  **시험 데이터는 코드로 만듭니다. 덤프 파일에 기대지 않습니다.**
    
    덤프는 스키마가 바뀌면 조용히 낡습니다. 코드로 만들면 컬럼이 늘어난 순간 깨져서 알려 줍니다. 테스트마다 필요한 것만 만들고, 다른 테스트가 만들어 둔 것에 기대지 않습니다.
    

### 7-3 어떻게 쓰는가

1.  **테스트 이름에 조건과 기대 결과를 적습니다.**
    
    `기사 나이가 만 65세를 넘으면 인수 거절로 판정한다`처럼 씁니다. `테스트1`이나 `calculateAge 테스트`로는 실패 목록만 보고 무엇이 깨졌는지 알 수 없습니다.
    
2.  **테스트 하나는 한 가지만 확인합니다.**
    
    여러 개를 한 테스트에 담으면 첫 번째에서 실패했을 때 나머지가 맞는지 알 수 없습니다.
    
3.  **준비·실행·확인 세 토막으로 나눠 씁니다.**
    
    무엇을 준비했고 무엇을 불렀고 무엇을 기대하는지가 눈으로 갈리면, 실패한 테스트를 읽는 시간이 짧아집니다.
    
4.  **경계값을 넣습니다.**
    
    기간의 첫날과 마지막 날, 0과 음수, 빈 목록, 최대 길이입니다. 하루 차이로 갈리는 계산은 가운데 값으로는 드러나지 않습니다.
    
5.  **테스트끼리 서로의 결과에 기대지 않습니다.**
    
    순서에 기대면 하나가 실패할 때 뒤가 전부 실패해, 원인이 하나인지 여럿인지 알 수 없습니다. 아무거나 하나만 골라 돌려도 통과해야 합니다.
    
6.  **시각에 기대는 로직은 기준 시각을 인자로 받게 만듭니다.**
    
    함수 안에서 현재 시각을 직접 읽으면 자정 근처에 결과가 달라지고, 테스트가 어느 날에는 통과하고 어느 날에는 실패합니다. 원인을 찾는 데 가장 오래 걸리는 종류입니다.
    

### 7-4 DB와 외부 시스템

1.  **DB가 필요한 테스트는 시험 전용 스키마에 붙입니다.**
    
    개발 DB를 함께 쓰면 남이 넣어 둔 데이터 때문에 결과가 달라지고, 반대로 테스트가 남의 데이터를 지웁니다. 접속 정보는 설정에서 읽어 시험용만 바라보게 합니다.
    
2.  **커넥션을 인자로 넘겨 부르는 테스트는 하나를 트랜잭션으로 감싸고 끝나면 되돌립니다.**
    
    지우는 코드를 따로 쓰지 않아도 테스트 사이가 깨끗해집니다. 리포지토리가 커넥션을 인자로 받기 때문에(5-6-2) 시험 코드가 연 트랜잭션을 그대로 넘길 수 있습니다.
    
3.  **서버를 띄워 HTTP로 부르는 테스트에는 2번이 통하지 않습니다. 정리 방법을 따로 정합니다.**
    
    시험 코드가 연 트랜잭션과 서버가 요청을 처리하며 잡는 커넥션은 서로 다릅니다. 시험 코드에서 되돌려도 서버가 넣은 행은 그대로 남고, 반대로 아직 커밋되지 않은 시험 데이터는 서버 쪽에서 보이지 않아 없는 것으로 처리됩니다. 이쪽은 실행할 때마다 스키마를 새로 만들거나, 테스트가 끝날 때 자기가 만든 것을 지우거나, 실행 단위마다 스키마를 갈라 쓰는 방법 중 하나를 정합니다. 정하지 않으면 시험 데이터가 쌓여 어제는 통과하던 테스트가 오늘 실패합니다.
    
4.  **외부 시스템은 갈아 끼운 구현으로 부릅니다.**
    
    벤더를 바꿔 끼울 수 있게 나눠 둔 그 자리를 씁니다. 테스트를 돌렸는데 실제로 문자가 나가거나 결제가 일어나면 안 됩니다.
    
5.  **외부 호출의 실패도 시험합니다.**
    
    시간 초과, 거절 응답, 잘못된 형식입니다. 성공만 시험하면 재시도할 실패와 하지 말 실패를 가르는 판단이 한 번도 실행되지 않습니다.
    

### 7-5 실행

1.  `npm test` **하나로 전부 돌아가게 합니다.**
    
    새로 온 사람이 무엇을 어떤 순서로 돌려야 하는지 묻지 않아야 합니다. 준비가 필요하면 그 준비까지 그 명령 안에 넣습니다.
    
2.  **단위와 통합을 따로 돌릴 수 있게 나눕니다.**
    
    단위는 몇 초, 통합은 몇 분입니다. 고치는 중에는 짧은 쪽만 반복해 돌리고, 밀어 넣기 전에 전부 돌립니다.
    
3.  **테스트는 코드를 밀어 넣을 때 자동으로 돌게 겁니다.**
    
    사람이 기억해서 돌리는 테스트는 급할 때 건너뜁니다. 급할 때가 가장 위험한 때입니다.
    
4.  **실패한 테스트를 남겨 두지 않습니다.**
    
    깨진 채로 둔 테스트가 하나 생기면 그다음부터 실패 목록을 아무도 읽지 않습니다. 고치거나, 지금 맞지 않는 테스트라면 지웁니다.
    

## 08\. 외부 연동 명세서 양식

밖으로 나가는 규격은 손으로 쓴 문서가 필요합니다. 6-3-5의 코드 생성은 우리 프론트가 쓰는 내부 API 얘기입니다. 아래를 복사해 채웁니다. 값은 예시입니다.

### 8-1 개정 이력

| 버전 | 일자 | 변경 내용 | 작성자 |
| --- | --- | --- | --- |
| 1.1 | 2026-09-06 | contracts[].status 에 ENDED 추가 | 홍길동 |
| 1.0 | 2026-08-20 | 최초 배포 | 홍길동 |

### 8-2 접근 정보

| 항목 | 값 |
| --- | --- |
| 운영 주소 | https://api.example.com |
| 테스트 주소 | https://api-test.example.com |
| 인증 방식 | 헤더 x-api-key |
| 자격증명 전달 | 담당자 메일로 개별 발급 |
| 호출 제한 | 분당 60회, 초과 시 429 |
| Content-Type | application/json; charset=utf-8 |
| 시각 표기 | ISO 8601 오프셋 포함 — 2026-09-06T14:22:10+09:00 |
| 오류 응답 | application/problem+json (RFC 9457) |
| 담당자 | 홍길동 · hong@example.com |

### 8-3 암복호화

| 항목 | 값 |
| --- | --- |
| 알고리즘 | AES-256 |
| 모드 | GCM |
| 패딩 | 해당 없음. CBC·ECB 등 블록 모드일 때만 적는다 (예: PKCS#7) |
| 초기화 벡터 | 12바이트 난수, 매 건 새로 뽑아 값 안에 포함 |
| 인증 태그 | 128비트, 암호문 뒤에 이어 붙여 마지막 조각에 함께 담는다 (GCM일 때) |
| 추가 인증 데이터 | 사용하지 않음 |
| 값 형식 | {알고리즘}.{모드}.{키}.{IV}.{암호문} |
| 키 이름 | pii-k1. 교체 시 사전 통보 후 양쪽 병행 기간을 둔다 |
| 인코딩 | base64url, 패딩(=) 제거 |
| 키 | 32바이트, Base64로 전달 |
| 대상 필드 | driver.ssn · driver.phone. 하이픈·공백을 지운 값을 암호화한다 |
| 키 전달 | 별도 경로로 전달. 이 문서에 적지 않음 |

### 8-4 공통 응답 형태

성공 여부는 HTTP 상태 코드가 알립니다. 본문에 따로 표시하지 않습니다.

```
성공 — 200 · 201
{ "receiptNo": "R2026-000481", "driverId": 3391 }

목록 — 200
{ "data": [ … ],
  "pagination": { "page": 1, "size": 20, "total": 137 } }

실패 — 4xx · 5xx        Content-Type: application/problem+json
{ "type": "https://api.example.com/problems/driver-not-found",
  "title": "기사를 찾을 수 없음",
  "status": 404,
  "detail": "접수 번호 R2026-000481 에 해당하는 기사가 없습니다",
  "instance": "/api/external/drivers/R2026-000481",
  "code": "DRIVER_NOT_FOUND" }
```

### 8-5 공통 에러 코드

| HTTP | code | title | 발생 조건 |
| --- | --- | --- | --- |
| 400 | INVALID_PARAMETER | 요청 값이 올바르지 않음 | 필수 누락, 형식 오류 |
| 401 | TOKEN_MISSING | 인증 정보가 없습니다 | 헤더 누락 |
| 401 | INVALID_TOKEN | 인증 정보가 올바르지 않습니다 | 키 불일치·만료 |
| 403 | FORBIDDEN | 권한이 없습니다 | 다른 업체의 자원 |
| 404 | NOT_FOUND | 대상을 찾을 수 없습니다 | 존재하지 않는 식별자 |
| 409 | DUPLICATED | 이미 처리된 요청입니다 | 중복 접수 |
| 429 | RATE_LIMITED | 호출이 너무 잦습니다 | 호출 제한 초과 |
| 500 | INTERNAL_SERVER_ERROR | 처리 중 오류가 발생했습니다 | 그 밖의 실패 |

### 8-6 API 목차

| 번호 | 이름 | 메서드 | 경로 |
| --- | --- | --- | --- |
| 1 | 기사 등록 | POST | /api/external/drivers |
| 2 | 기사 목록 조회 | GET | /api/external/drivers |
| 3 | 기사 해지 | POST | /api/external/drivers/{driverId}/cancel |

### 8-7 API 상세

#### 1\. 기사 등록

| 항목 | 값 |
| --- | --- |
| 메서드 | POST |
| 경로 | /api/external/drivers |
| 설명 | 기사를 등록하고 접수 번호를 돌려줍니다. 접수 시점에 심사는 하지 않으며, 결과는 2번으로 조회합니다 |

#### 요청

| 필드명 | 타입 | 길이 | 필수 | 예시 | 설명 |
| --- | --- | --- | --- | --- | --- |
| companyId | number | - | Y | 12 | 업체 번호 |
| driver | object | - | Y |  | 기사 정보 |
| driver.name | string | 20 | Y | 홍길동 | 기사 이름 |
| driver.phone | string | 200 | Y | aes256.gcm.… | 암호화 대상. 하이픈을 지운 뒤 암호화한다 |
| driver.ssn | string | 200 | Y | aes256.gcm.… | 암호화 대상. 하이픈을 지운 뒤 암호화한다 |
| driver.birthday | string | 8 | Y | 19900101 | YYYYMMDD |
| contracts | array | - | N |  | 계약 목록. 없으면 빈 배열 |
| contracts[].no | string | 20 | Y | C2026-0001 | 계약 번호 |
| contracts[].startAt | string | 25 | Y | 2026-09-06T00:00:00+09:00 | 계약 시작. 시작일 포함 |
| contracts[].status | string | 20 | Y | ACTIVE | ACTIVE · ENDED |
| memo | string | 500 | N |  | 비고 |

```
{
  "companyId": 12,
  "driver": {
    "name": "홍길동",
    "phone": "aes256.gcm.pii-k1.7AOkv9uITst8dabB.60jHw3PJo6bKEO7GppfPSBuCmfsXlD3AhEq5",
    "ssn": "aes256.gcm.pii-k1.8tlH7J3tYj6ukBOl.Lq5-BJ_Ih1y5BxryVeVfKomDLADzXnKZ0FHm_tM",
    "birthday": "19900101"
  },
  "contracts": [
    { "no": "C2026-0001", "startAt": "2026-09-06T00:00:00+09:00", "status": "ACTIVE" }
  ]
}
```

#### 응답

| 필드명 | 타입 | 길이 | 필수 | 예시 | 설명 |
| --- | --- | --- | --- | --- | --- |
| receiptNo | string | 20 | Y | R2026-000481 | 접수 번호. 2번 조회에 쓴다 |
| driverId | number | - | Y | 3391 | 기사 번호 |
| receivedAt | string | 25 | Y | 2026-09-06T14:22:10+09:00 | 접수 시각 |

```
201 Created
{
  "receiptNo": "R2026-000481",
  "driverId": 3391,
  "receivedAt": "2026-09-06T14:22:10+09:00"
}
```

```
409 Conflict        Content-Type: application/problem+json
{
  "type": "https://api.example.com/problems/duplicated-driver",
  "title": "이미 등록된 기사",
  "status": 409,
  "detail": "같은 업체에 같은 주민등록번호가 이미 등록되어 있습니다",
  "instance": "/api/external/drivers",
  "code": "DUPLICATED_DRIVER"
}
```

#### 에러 코드

| HTTP | 코드 | 발생 조건 |
| --- | --- | --- |
| 409 | DUPLICATED_DRIVER | 같은 업체에 같은 주민번호가 이미 등록됨 |
| 422 | CONTRACT_DATE_INVALID | startAt 이 오늘보다 이전 |
| 422 | AGE_NOT_ALLOWED | 나이가 인수 기준을 벗어남 |

> **참고** — 필드명은 중첩을 `parent.child`, 배열 안을 `list[].item`으로 적습니다. 들여쓰기로 표현하면 복사해 붙일 때 깨지고 검색으로 필드를 찾을 수 없습니다. 길이는 문자열에만 적고, 숫자는 범위를, 날짜·시각은 형식을 설명 칸에 적습니다. 값이 정해진 필드는 가능한 값 전체를 적습니다. 필수 여부가 다른 필드에 따라 갈리면 `Y`·`N` 대신 조건을 설명 칸에 적습니다.
